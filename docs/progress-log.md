# Прогресс: миграция Свиночервя на общую модельку (WormModel/WormRenderer)

## Правка 61 (2026-08-31): кухня — нож слушается пальца, доска едет вправо, склянки в ряд

Семь пунктов третьего живого прогона. Общая тема у пяти из них одна:
**невидимое перебивает видимое** — то CSS-анимация перебивает transform,
то отработавшая зона захвата съедает чужие касания.

### Нож ехал в другую сторону

Свайп вверх опускал нож, свайп вниз поднимал. Причина арифметическая:
ось поворота у рукояти справа, поэтому ПОЛОЖИТЕЛЬНЫЙ угол поднимает
левый конец (лезвие), а в формуле угол складывался с ходом пальца вниз.
Теперь `KNIFE_UP: 44`, `KNIFE_DOWN: -3` и минус в формуле:

    this.knifeStep(d.base - (p.y - d.from.y) * this.KNIFE_SENS);

Заодно разведены кламп и проверки «взвода/реза»: раньше `chopOnce()` мог
сработать от сырого угла ДО ограничения, и один длинный свайп давал два реза.

### Нож дёргался

Угол ставился атрибутом каждый кадр указателя — а событий указателя
приходит меньше, чем кадров. Плавность отдана CSS
(`#kt-knife-arm { transition: transform 0.07s linear }`), это дешевле
собственного rAF и не спорит с ним, потому что позиция ножа живёт на
ДРУГОМ уровне группы. Уровни теперь строго разделены: позиция — на
`#kt-knife`, покачивание — на обёртке, замах — на `#kt-knife-arm`.

### Призрак ножа

`.kt-done` задавал `opacity: 0.55` в CSS, а код гасил нож атрибутом
`opacity="0"` — CSS сильнее, нож оставался висеть полупрозрачным поверх
плиты. Прозрачность из CSS убрана целиком, гашение делает `hideKnife()`.

### Ножом можно было шевелить после нарезки

Нарезка кончилась, а нож всё ещё ловил касания и закрывал собой доску и
бутыли. Добавлен `parkKnife()`: отбирает ввод (`this.locked = true`),
отправляет нож на своё место у стены и через 520 мс возвращает управление.
`pointer-events: none` на группе оказалось мало — у зон захвата свой
`pointer-events="all"`, поэтому правило нужно и на потомков:

    #gluttony-game #kt-knife.kt-done,
    #gluttony-game #kt-knife.kt-done * { pointer-events: none; }

### Удержание доски заменено сдвигом

После выкладки продуктов игра молчала: подсказки не было, а перейти к
нарезке можно было только удержанием — жест, о котором неоткуда узнать.
Механика удержания (`startHold`, `cancelHold`, `#kt-hold` и его стили)
удалена целиком. Теперь доску просто **тащат вправо** — тем же жестом,
которым игрок уже двигал продукты, и с той же индикацией: появляется
кольцо-цель справа, доска за пальцем едет, за порогом
(`x > board.fridge.x + 110`) начинается нарезка.

Вывод на будущее: если жест нельзя показать пиктограммой, его в игре без
слов быть не может. Удержание показать нечем — значит, его нет.

### Склянки висели друг на друге, кран не нажимался

Три бутыли стояли почти в одной точке при полуширине 28 — верхняя
накрывала соседей, а кран оказался под ними. Расставлены в ряд с шагом 58
(`752 / 810 / 868`), кран поднят на `y: 596`, выше их голов.

### Зона кастрюли

`potZone` расширена вверх: `{x:428, y:468, w:214, h:336}`. Кастрюля
маленькая и стоит низко, а ингредиент игрок отпускает над ней — теперь
зона накрывает и воздух над кастрюлей.

### Проверено

`tools/test-kitchen.js` — полный проход: 12 надрезов, налито 60,
накормлен, шкала 100, жетон 1, кучка 12. `tools/test-no-words.js` — все
разделы пустые.

## Правка 60 (2026-08-27): кухня — доска как предмет, нарезка всего сразу

Переделка по второму живому прогону. Главное решение — **два слоя координат**.

Кухня живёт в сцене 900×1948, по ней ездит камера. А доска, нож и то, что на
них лежит, живут в координатах ЭКРАНА и камере НЕ подчиняются. Так и должно
быть: доска — предмет, который игрок держит перед собой, она ближе камеры.
Раньше она лежала в сцене, и её положение приходилось бы пересчитывать под
каждый наезд — она то пряталась за плитой, то уползала за край.

Из этого само собой вышло то, чего просил пользователь: ход игры читается
одним непрерывным движением по кухне. Доска въезжает снизу-справа, набирается
продуктами, уезжает, снова въезжает уже на столе для нарезки, потом
поднимается к плите и встаёт на столешницу слева от неё.

**Что теперь как.**

* **Холодильник**: три полки по типам — мясо, овощи, зелень. Тап по открытой
  дверце закрывает его и отпускает камеру на общий вид.
* **Продукты ПЕРЕТАСКИВАЮТ**, а не тапают: под пальцем продукт подрастает,
  иначе прячется под ним же. Тап ничего не сообщает о том, куда предмет
  денется, — перетаскивание показывает это само.
* На доске **по одному каждого типа**; пищеблок кладётся один и ни с чем не
  сочетается. Взятый продукт остаётся на полке **силуэтом** с числом — видно,
  что именно взяли и что это можно вернуть, перетащив обратно.
* **Уход на нарезку — удержание доски** с заполняющимся кольцом. Тап по ней уже
  занят (кладут и снимают продукты), а держать — жест, который ни с чем не
  путается.
* **Режется всё сразу**, одной кучей: цвет кусков берётся из типов на доске,
  поэтому по куче видно состав блюда. Отдельного рисунка на каждую комбинацию
  не нужно.
* **Нож перерисован шеф-ножом** и работает как настоящий: ось вращения у торца
  рукояти, свайп поднимает и опускает лезвие. Один надрез = ПОЛНЫЙ размах,
  вверх и вниз, — иначе можно было бы дрожать у доски и нарезать всё за
  секунду.
* **Доску тянут вверх** — она уходит к плите. **Жидкость держат НАД кастрюлей**,
  и пока держат, течёт струя и уровень растёт: не «отпустил и появилось».
* **Отдельный наезд на кастрюлю** для помешивания, и мешают теперь СЛЕВА
  НАПРАВО — это движение по кругу в кастрюле, а не рубка.
* **Кастрюлю тянут вниз** — оттуда кормёжка.

**Три бага, и все три одного рода: невидимая зона захвата съедает чужие
касания.**

1. Зона ножа поворачивается вместе с лезвием, и на поднятом ноже узкая полоса
   уходила из-под пальца — нож не брался вовсе.
2. Отработавший нож оставался лежать поверх доски и забирал себе все нажатия:
   доску потом было не поднять, хотя резать уже нечего.
3. И самое неочевидное: `pointer-events: none` на группе ножа НЕ помогало,
   потому что у зон захвата стоит собственный `pointer-events: all`, а он
   перебивает выключение на родителе. Нож, отведённый в угол, продолжал ловить
   касания и отбирал их у бутылей на другом конце кухни. Лечится правилом и на
   группу, и на всё внутри неё.

**Тест тоже был неправ:** он ждал, пока перестанет меняться АТРИБУТ камеры, а
двигает картинку CSS-переход — атрибут замирает сразу, и координаты считались
на лету. Теперь ждём реальную матрицу отрисовки.

## Правка 59 (2026-08-27): кухня — игрок не понимал НИЧЕГО, чиним обратную связь

Первый живой прогон кухни провалился целиком, и это самая полезная правка за
всё время: механика работала, а игра при этом молчала. Игрок жал по продуктам
в холодильнике — ничего; ушёл к столу, только случайно ткнув мимо; нож качался
сам по себе и не реагировал на палец; у плиты застрял насмерть, не поняв, что
воду вообще можно налить.

Вывод, который стоит записать шире, чем про кухню: **проверять надо не то, что
триггеры срабатывают, а то, что игрок ВИДИТ их срабатывание.** Прошлый тест
проходил цикл до конца и был доволен — он-то знал, куда тыкать.

**Что было сломано на самом деле.**

* **Взятый продукт улетал за край экрана.** Он летел с полки сразу к столу, а
  стол в кадре наезда на холодильник не виден. Тап выглядел несработавшим,
  игрок бил по полке снова — и набирал четыре куска мяса, не подозревая об
  этом. Появилась КОРЗИНА на полу перед холодильником: продукт летит в неё по
  дуге, корзина подпрыгивает, взятое видно и сосчитано.
* **Выход из холодильника был тапом в пустоту.** Теперь его несёт корзина:
  тап по ней = «понёс к столу», и продукты переезжают на стол вразнобой по
  времени, чтобы взгляд их вёл.
* **Нож не реагировал на палец.** CSS-анимация покачивания и transform замаха
  дрались за один и тот же атрибут, и анимация побеждала. Разведено на три
  уровня: позиция, покачивание вхолостую, замах. Плюс нож теперь ЕДЕТ ЗА
  ПАЛЬЦЕМ — он и есть палец, а не отдельная картинка сбоку.
* **Нож был нарисован вверх ногами**: над мясом висела рукоятка.
* **Ложку и лейку нельзя было взять** — тонкие, попасть по ним пальцем почти
  невозможно. Зоны захвата теперь у всего, что берут.
* **Указатель ставился, но не двигался.** Та же беда, что с ножом: анимация
  пульса перетирала координаты. И `transform-box: view-box` вместо `fill-box`
  заставлял кольцо пульсировать вокруг центра ХОЛСТА, уезжая от цели.
* **Группа ножа осталась незакрытой** после переделки, и указатель с ложкой
  уехали внутрь неё — оттого ложка и не нажималась.

**Что добавлено.**

**Указатель** — единственная подсказка, возможная в игре без слов. Кольцо
пульсирует на том, чего надо коснуться; пунктирная дуга с бегущим пунктиром
означает «перетащи отсюда туда»; вертикальная черта — «води вверх-вниз».
Появляется через 1.4 секунды бездействия, гаснет от любого касания и
перезаводится не только на касание, но и на СМЕНУ СОСТОЯНИЯ — иначе он
показывал бы уже сделанный шаг. Куда показывать, решает одна таблица
`nextStep()`: если шаг некуда показать, значит шага и нет, и это видно сразу в
коде, а не на живом игроке.

**Отклик на касание.** Любой предмет, которого коснулись, отвечает движением:
подпрыгивает, если сработало, дёргается, если нельзя (кончился запас, корзина
полна, кучку кладут раньше основы). На общем виде работает вся кухня целиком —
куда ни ткни из четырёх мест, игра ведёт к холодильнику.

## Правка 58 (2026-08-27): кухня перерисована вручную и стала диегетической

По эскизу пользователя. Две большие перемены разом: своя графика вместо эмодзи
и интерфейс, в котором нет ни одного меню.

**Вся кухня — один SVG.** `src/minigames/gluttony/kitchen-art.js` рисует
сцену целиком: холодильник с морозилкой, плита с духовкой и двумя конфорками,
кастрюля на левой конфорке, бутыли на столешнице, раковина с посудой и
выдвижной лейкой (правым краем уходит за кадр, как на эскизе), окно с деревом,
стол с доской и ножом на переднем плане. Продукты тоже свои: у каждого целый
вид и две стадии нарезки — крупные куски и почти масса. Стадии отличаются не
цветом, а числом и размером кусков, поэтому на продукт хватает одной тройки
цветов.

Цвета — новое семейство `PALETTE.kitchen`. Отдельное, а не подбор из
существующих рамп: вся палитра построена вокруг живого тела червя, и морковь
цветом плоти читалась бы частью персонажа, а не предметом рядом с ним.
Толщины линий — общая лестница `STROKE`, умноженная на два: сцена нарисована в
двойном размере стейджа, и контур 2.6 на общем виде превратился бы в 1.3, то
есть в другую ступень.

**Интерфейс диегетический.** Нет ни кнопки, ни списка, ни всплывающей панели.
Тапнул по холодильнику — дверцы повернулись вокруг своих петель, продукты
лежат на полках с числом под каждым. Взял — продукт перелетел на стол.
Тапнул мимо — холодильник закрылся, камера поехала к столу. Тащишь продукт на
доску, водишь ножом, кучка едет к плите. Тащишь бутыль или лейку к кастрюле —
льётся основа. Кидаешь кучки в кастрюлю пальцем. Мешаешь ложкой.

Из этого следует главное техническое решение: ВСЁ живёт в координатах сцены
(780×1688), включая перетаскиваемое, а экранные пиксели переводятся туда
матрицей SVG. Тогда предмет под пальцем лежит ровно под пальцем при любом
наезде камеры, и чинить это отдельно для каждого масштаба не нужно.

**Камера не переключает экраны**, а наезжает на участок одной картинки.
Прямоугольники наезда вписываются целиком, поэтому вокруг предмета всегда
виден кусок кухни.

**Три бага, которых глазами не видно, поймал тест.**

1. **Кучка из кусков дырявая.** Нарезанный продукт — это отдельные куски с
   промежутками, и палец, попавший между ними, проваливался мимо: предмет не
   брался, хотя игрок целился в середину. Проявлялось как «иногда одна кучка
   не кладётся в кастрюлю». Лечится невидимой зоной захвата по габаритам
   (прозрачная заливка, а не `none`: `none` событий не ловит).
2. **Холодильник закрывался сам** через полсекунды после первого взятого
   продукта — и второй с третьим взять было некуда, полки уезжали из-под
   пальца. Теперь закрывает игрок, и это же его «я набрал».
3. **Кучку можно было взять дважды.** Пометка снималась, только когда узел
   уходил из DOM, а он ещё полсекунды доезжал до кастрюли и гас.

Плюс мелочи: огонь и пар горели с первого кадра (кейфреймы сами правят
прозрачность и перебивали выключенное состояние — теперь анимация включается
классом); `INK` и `K` в файле графики конфликтовали с глобальными именами
рендерера червя.

**Тест переписан под тот же диегетический ввод**: он не жмёт селекторы, а
таскает предметы мышью по координатам сцены — заодно это проверка, что перевод
«экран → сцена» не врёт. И научен ждать ОСТАНОВКИ камеры: пока наезд идёт,
координаты под пальцем меняются каждый кадр, и тест то попадал, то нет.
Три прогона подряд проходят одинаково; слов на кухне по-прежнему ноль.

## Правка 57 (2026-08-27): чревоугодие — кухня собрана и работает

Первая правка по плану `20-gluttony-kitchen.md`: не проектирование, а код.
Старые «стол → ведро → кормёжка» заменены кухней из трёх мест.

**Что сделано.**

* `src/config/kitchen.js` — каталог данными, как снаряжение гнева: ингредиенты
  с типом и числом качелей ножа, жидкости, таблица качества блюда, стартовая
  кладовая. Ни одной функции — файл поедет на сервер как есть.
* Кладовая в состоянии (`data.pantry`), отдельно от `inventory`: там
  снаряжение, тут расходники. Стартовый запас выдаётся ОДИН раз по флагу
  `player.pantry_seeded`, а не «досыпается, если пусто» — иначе игрок,
  честно израсходовавший продукты, получал бы их обратно при каждой
  перезагрузке.
* Валюты `glut_shard`/`glut_token` с тем же разменом «три в один», что у
  гнева, и `dung` — какашки одним целым числом в мелких.
* `Backend.dishQuality(meta)`, `spendIngredients()`, размер кучки в
  `digestion.poop_size`, начисление какашек в `removePoop()` (там ровно на
  этом месте и стоял комментарий «станет ресурсом — начисление добавится
  здесь же»).
* Сама кухня: общий вид → холодильник → доска → плита → кормёжка. Камера
  ездит по ОДНОЙ ленте из трёх мест, а не переключает экраны. Мини-игра
  переведена на общее окно (`MinigameWindow.attach`) — инвариант 8, минус
  одна самодельная рамка из шести.
* Этап кормёжки не тронут: наклон кастрюли, капли и раскладка по реальным
  габаритам персонажа работают, переписывать их незачем.

**Найдено тестом и починено:** палец не отпускают ровно в тот момент, когда
кончился последний ингредиент — качели продолжали считаться ещё секунду, пока
камера едет к плите, и клали в кучки `undefined`. Одиннадцать ошибок подряд на
отрисовке. Теперь `onChopSwing()` выходит сразу, если резать больше нечего.

Ещё две мелочи с той же проверки: у бутыли не было `position: relative`, и
число бутылок рисовалось не у своей бутылки, а внизу колонки; пол сцены лежал
ПОВЕРХ ленты и обрезал мебель по колено.

**Проверено машиной** (`tools/test-kitchen.js`): полный цикл проходит, блюдо
из трёх типов на бульоне даёт три осколка (сразу разменянные в жетон), кучку
на 12 мелких, кладовая списывается ровно на потраченное. Слов на кухне нет —
`tools/test-no-words.js` теперь обходит и её.

**Чего пока нет:** магазин-смартфон, апгрейды кухни (по решению — после живых
тестов), бусты жидкостей, свои спрайты ингредиентов вместо эмодзи и приход
продуктов из сада и от боссов.

## Правка 56 (2026-08-27): похоть — ванная, финал на меткость, числа подобраны

Идея от пользователя: ванная, четыре этапа мытья, финал с прицеливанием.
Записано в `docs/plan/21-lust-bath.md`, считает `tools/sim-lust.js`. Кода нет.

**Чем идея сильна.** Финал перестаёт быть анимацией-наградой и становится
игрой с потолком мастерства — того, чего не хватало почти всем мини-играм.
Прокачка при этом двигает не абстрактную цифру, а ощущение управления: сила
струи, разброс угла, податливость хвоста. Магазин доступен до и после забега,
но не во время — подготовился, отыграл, потратил.

**Решение пользователя по случайности: подсказок не будет.** Я предлагал
телеграфировать следующий толчок замахом, чтобы промах был виной игрока, а не
кубика. Ответ: случайность — это ТОВАР, она и покупается прокачкой; видел бы
игрок разброс заранее, сужать его было бы незачем. Принято, и записано как
правило.

Но у такого решения одно жёсткое условие, и его пришлось проверять расчётом:
**на старте забег обязан в среднем приносить хотя бы один осколок**, иначе
новичок играет впустую, а прокачка, которая должна его вытащить, покупается за
то, чего у него нет. Числа подобраны ровно под это: старт даёт **1.00
осколка**.

| Ступень | Разброс | Попаданий | Осколков | Пустых забегов | Жетон целиком |
|---|---|---|---|---|---|
| старт | ±25° | 2.5 | 1.00 | 24.5% | 1.9% |
| 1 | ±20° | 3.7 | 1.58 | 6.9% | 11.8% |
| 2 | ±15° | 5.9 | 2.55 | 0.2% | 61.5% |
| 3 | ±10° | 9.0 | 3.00 | 0.0% | 99.8% |

Два захода в сутки (похоть пустеет за 12 часов): от 0.67 до 2.00 жетона, рост
втрое — как в кухне и на дорожке тщеславия.

**Ступеней ровно три, и это расчёт.** Пятая ступень давала те же 3.00 осколка:
шкала упирается в потолок, покупать дальше нечего. Лестница кончается там, где
жетон становится почти гарантией.

**Отмечено к живым тестам:** на старте каждый четвёртый забег даёт ноль. Для
чистой случайности нормально, и на первой ступени падает до 7%. Если окажется
обидно — самое дешёвое лечение — первая секция от одного попадания вместо
двух: потолок и остальной баланс не двигаются.

**Прочие поправки, вошедшие в план.** Мыло и мочалка разведены по вводу
(широкие мазки против коротких тёрок) — иначе игрок делает одно движение
дважды подряд. Покрытие тела считается по порогу 90–95%, а не под ноль:
«закрась всё» всегда упирается в пару пикселей. Пузырей 8–12, лопаются пачкой.
Хвост как отдельная сущность — правильно сейчас, но записан долгом: при
взрослении червя он за ним не изменится.

## Правка 55 (2026-08-27): чревоугодие — кухня, разбор и решения

Идея от пользователя: кухня с холодильником, доской, плитой; готовка вручную;
качество блюда решает размер какашки и число жетонов. Записано в
`docs/plan/20-gluttony-kitchen.md`. Кода нет, это проектирование.

**Чем идея сильна.** Кухня склеивает игру: овощи из лени, мясо из гнева,
напиток из зависти, специи из сада. Ни одна другая мини-игра так не связана с
остальными. Пищеблок в морозилке снимает тупик «нечем кормить» и при этом не
даёт какашку — потребность закрыта, экономика не двинулась. Качество блюда
считается по числу РАЗНЫХ типов ингредиентов, поэтому рецептов заучивать не
надо и слов не нужно. И вместимость холодильника закрывает дыру, найденную в
саду: сад производит быстрее, чем червь ест, а без потолка склада овощи
обесценились бы.

**Возражения, которые пользователь снял, и его доводы — они важнее моих:**

* Я предупреждал про утомление от долгой готовки. Ответ: цикл и так укладывается
  в минуту, а медленный старт НАМЕРЕННЫЙ — если сразу сделать готовку удобной,
  прокачке нечего будет чинить. Записано как правило: апгрейд обязан
  одновременно сокращать время И улучшать результат.
* Я предлагал ронять мясо с обычных побед, чтобы снять дефицит. Ответ: дефицит
  намеренный, он и гонит в забег. С обычных побед мяса нет. Общее правило:
  не пользуешься всеми механиками — качаешься медленнее.
* Апгрейды кухни не проектируем заранее — сначала голые механики, чтобы было
  что тестировать вживую.

**Лучшая мысль из обсуждения — про бусты от жидкостей.** Их задача дать
возможность заходить реже, но чтобы реже заходить, надо заранее подготовиться:
вырастить, добыть, купить, сварить. Игрок получает ОЩУЩЕНИЕ экономии времени,
а проводит в игре примерно столько же, и остаётся доволен. Граница при этом
жёсткая: буст не покупает силу, только время и удобство.

**Жетоны — на механизме гнева.** Пищеблок и блюдо из одного типа дают осколок,
два типа — два осколка, полное блюдо (три типа плюс жидкость кроме воды) —
целый жетон. Размен «три осколка = жетон» уже написан (`ECONOMY.exchange`).
Три кормёжки в сутки: ленивый игрок получает жетон в сутки, старательный —
три.

## Правка 54 (2026-08-27): сад лени — цикл разрезан надвое, инструменты проверены

Пользователь задал цикл: четыре часа, из них три до прополки, каждый час
снимается одной какашкой (можно снять все три), после прополки свой таймер,
который сокращают только грабли. Разбор пересчитан под это —
`docs/plan/19-sloth-garden.md`, считает `tools/sim-garden.js`.

**Разрезанный цикл снял противоречие прошлой правки.** Там вышло, что часы не
работают, а минуты работают. На деле нужны оба: этап 1 в часах отвечает за
«зашёл раз в день, всё выросло», этап 2 в минутах — за «сижу в саду, вожусь».
И главное: **удобрение не сокращает ожидание, а покупает его целиком** —
поэтому оно работает в часах, где инструмент бы не сработал.

**Правило, которое надо помнить при любой правке чисел:** инструмент,
сокращающий ожидание, стоит ровно столько, сколько игрок готов ждать НЕ УХОДЯ.
Расчёт: 25 и 15 минут дают одинаковые 1.48 урожая в сутки, потому что игрок в
промежутке всё равно уходит на шесть часов.

**Грабли — пороговый апгрейд, и мерить его надо полосой, а не точкой.**
Терпение не константа игры, а свойство человека: у одного три минуты, у
другого пятнадцать. Как только этап 2 укладывается в терпение, урожай
прыгает РОВНО ВДВОЕ (1.48 → 2.98): игрок пропалывает, досиживает и собирает в
тот же заход. Ступени должны спускаться сквозь всю полосу: 25 → 12 → 6 → 3.

**Лейка не ускоряет, а удешевляет.** Она режет базу этапа 1 целыми часами
(3 → 2 → 1), и от этого втрое дешевеет полный скип: 9 → 6 → 3 какашки в сутки.
На суточном запасе в три какашки это чувствуется мгновенно. Оговорка:
последняя ступень делает скип этапа 1 однокакашечным, то есть это потолок и
стоить он должен соответственно.

**Лопата под правило порога не попадает вообще** — она даёт находки, а не
время, и потому работает при любом расписании игрока.

**Удобрение убыточно по ресурсу, и это правильно.** Скип всего этапа даёт +34%
урожая (1.48 → 1.98), стоит ровно весь суточный запас не-садовода, а плодами
возвращает 1.25 из потраченных трёх. Платят за ВРЕМЯ, а не за прибыль — значит
круг «удобрение → урожай → блюдо → какашки» не печатает ресурсы. Отсюда
жёсткое правило: цена плода в какашках обязана оставаться ниже цены снимаемого
часа.

**Потолок грядок сменился и стал лучше.** Раньше его задавали руки (пока
копаешь одну, остальные ждут). Теперь этап 1 идёт оффлайн и параллельно, так
что ограничивают суточный запас какашек и аппетит червя — то есть то, чем
игрок и так занимается.

## Правка 53 (2026-08-27): лень — сад с грядками, разбор и числа

Идея от пользователя: вместо горшка участок с грядками, семена разных видов,
инструменты с прокачкой, какашки как внутренняя валюта, плоды уходят в
чревоугодие и возвращаются какашками. Разбор — `docs/plan/19-sloth-garden.md`,
считает `tools/sim-garden.js`. Кода нет, это проектирование.

**Главная находка: масштаб времени решает всё.** Первый расчёт поставил
ожидания в часы (полив 4 ч, прополка 3 ч) — и прокачка инструментов дала
ровно ноль: 1.47 урожая в сутки и на стартовых, и на лучших. Причина в том,
что за один заход грядка продвигается ровно на одну стадию, сколько бы ни
ждала: следующая стадия начинает отсчёт заново, и урожай упирается в число
заходов, а не в скорость. Та же дыра, что в зависти.

Значит ожидания живут в МИНУТАХ, и сад — игра, в которой сидят: пока одна
грядка тянется, копаешь вторую. С минутами прокачка работает: одна грядка
даёт 1 урожай за десятиминутный заход на стартовых инструментах и 4 на
лучших. Оффлайн не отменяется — растение считается от метки времени
(инвариант 1), просто это страховка, а не единственный путь. Час из идеи
никуда не делся: он становится длиной цикла у ДЕРЕВЬЕВ, а овощи крутятся за
три минуты.

**Шкала лени не может расти от присутствия.** Две причины: игровая — лучшей
стратегией становится открыть сад и положить телефон, то есть механика платит
за то, что игрок не играет; техническая — растущий сам счётчик состояния это
тикающий таймер, запрещённый инвариантом 1. Расти должна ЗА ДЕЙСТВИЯ, а
поскольку действия нарочно медленные (23 секунды ручной работы на цикл),
ощущаться будет как время.

**Грядок имеет смысл не больше десяти.** Ограничитель — руки: 1–5 грядка дают
по +2 урожая, 6–10 по +1, одиннадцатая ноль.

**Какашки хранить одним целым числом в мелких**, номиналы рисовать при показе:
тогда размен и сдача получаются бесплатно. Но награды к номиналам привязывать
нельзя — «простое блюдо мелкая, хорошее крупная» это разрыв в сто раз между
соседними обедами. Награда числом (1 / 4 / 12 / 30), номинал выбирается сам.

**Узкое место — поставка.** Чревоугодие пустеет за 8 часов, значит три
кормёжки в сутки, значит три мелких какашки — весь бюджет экономики лени.
Отсюда цены в мелких, а не в средних; отсюда же выгода замкнуть круг (садовод
получает 10–35 против трёх). И лень нельзя запирать за чревоугодием: при
копании изредка попадается старая засохшая кучка — ворота сняты бессловесно.
Обратное узкое место тоже есть: плодов сад даёт больше, чем червь съедает,
поэтому блюдо из нескольких плодов и потолок склада.

**Удобрение окупается только внутри сессии** — оно покупает минуты, которые
игрок проведёт в саду прямо сейчас. Правило балансировки: никогда не делать
его выгодным всегда, иначе выбора нет и это лишний тап.

**Растение обязано выводиться из СИДА.** Сейчас `PlantModel.generate()` тянет
всё из `Math.random()`, а растение живёт часами и лежит в сохранении — то есть
после каждой перезагрузки страницы оно выглядело бы по-новому. Вид при этом —
таблица ограничений (полоса оттенка, набор стеблей, форма листа, плод, длина
цикла), тот же приём, что у локаций.

**Растения не гибнут.** Тамагочи уже наказывает шкалами, второй механизм —
двойной штраф за одну провинность. И смерть червя должна остаться единственной
смертью в игре: она и есть ставка, ради которой возвращаются.

## Правка 52 (2026-08-27): зависть — добыча, риск и прокачка, две находки

Идея от пользователя: значимые образы дают ресурсы независимо от цвета, за
внутреннюю валюту качается частота ценных образов, число ходов и несгораемые
деления шкалы; вынос добычи — только при победе. Разбор и числа —
`docs/plan/18-envy.md`, считает `tools/sim-envy.js`. Кода нет, это
проектирование.

**Играет на руку то, что уже есть:** подсказка о цвете приходит на первой
секунде удержания, а выбор засчитывается на второй — палец можно отпустить.
То есть «нашёл значимый образ → понял, нужен ли → решил» уже в коде.

**Главная поправка: цель и ходы — разные числа.** Сейчас `rounds: 3` означает
и число делений шкалы, и число выборов, отсюда тупик «все три обязаны быть
зелёными». Развести: шкала — цель, ходы — бюджет, качается бюджет. И заодно
сменить правило шкалы. Перебором посчитано, что при «цель 3, зелёный +1,
красный −1» каждый красный стоит РОВНО ДВА хода, поэтому каждая вторая ступень
прокачки не даёт ничего. При «шкала 6 делений, зелёный +2, красный −1» красный
дешевле зелёного, и почти каждый купленный ход что-то меняет: 5 ходов — один
красный, 6 — два, 8 — три.

**Проигрыш переопределён.** Предложенное «красный при нулевой шкале» ломается
о собственную прокачку: первое же несгораемое деление делает пустую шкалу
недостижимой. Условие должно быть «кончились ходы, а шкала не полна» — тогда
порог остаётся страховкой, а не отменой правил. Сгорает при провале ДОБЫЧА;
шкала греха капает и за провал, иначе теряется гарантия.

**Находка 1: провал не наступает вообще.** Симуляция аккуратного игрока даёт
0% провалов на всех ступенях — зная цвет заранее, он никогда не возьмёт
неотыгрываемый красный. Это не отменяет проигрыш, но задаёт его смысл: он
наказывает просчёт, а не невезение.

**Находка 2: в текущем коде жадность ОБЛЕГЧАЕТ игру.** `wrongCountFor(fill)`
считает число красных от заполнения шкалы. Взял красный → шкала просела →
следующее облако стало реже → читать легче. Считать надо от числа сделанных
ходов: тогда длинный жадный забег честно тяжелеет. Правка однострочная.

**Чего не качать: число зелёных образов.** Один правильный среди многих и есть
загадка; два снимают напряжение целиком. А вот число КРАСНЫХ качать можно — это
редкий случай прокачки, которая одновременно награда и усложнение.

**Экономика.** Золото не должно приходить только с образов: на старте это 0.6
за забег, и зависть оказалась бы самым бедным грехом при самом длинном забеге.
Победа платит по общему правилу (`goldBase: 10`), образы добавляют сверху —
получается от 10.6 до 23.3 золота. Внутренняя валюта (глаза 👁) от 1.2 до 3.7
за забег, цены ступеней 5 → 9 → 16 → 29 → 52.

## Правка 51 (2026-08-27): тщеславие — ковровая дорожка, ажиотаж, три покупки

Нарративная рамка от пользователя, и она чинит не только вид. Червь выходит из
машины у начала ковровой дорожки, жмёт старт и идёт двадцать секунд; сам он
стоит в центре экрана, а дорожка и толпа за ограждениями едут вниз, уходя
вглубь. Толпа — папарацци: зона МИМО червя это их вспышка, зона НА черве —
кто-то перегнулся через ограждение и чмокнул.

**Что рамка даёт механике.** Таймер заменяет шкалу: прогресс — это сама
дорожка, а отдельный счётчик 0..100 исчезает вместе с победой и поражением
(одной сущностью в бессловесном интерфейсе меньше). Проигрыша нет: дойти
нельзя не суметь, шкала греха закрывается всегда, а собранное внимание решает
только размер выплаты.

**Ажиотаж вместо серии — и вот почему.** Первая версия правила была «серия
попаданий подряд, промах обнуляет». Написал `tools/sim-pride.js` (полный
перебор невозможен, поэтому двадцать тысяч выходов на клетку) — и правило
похоронилось: при точности 85% серия рвётся раз в семь попаданий, множитель
выше трёх не набирается, и покупка машины не даёт НИЧЕГО, 9.8 поцелуя против
10.4 на топовой. Заменено на ажиотаж: попадание +1, промах −2, но не в ноль;
множитель = 1 + ажиотаж/3, потолок задаёт машина. Теперь потолок упирается
по-настоящему.

Вспышки при этом нельзя игнорировать: денег они не дают, но держат ажиотаж, а
ажиотаж — это цена поцелуя.

**Три покупки бьют в разные места цикла:** толпа даёт объём (зоны чаще:
1000 → 550 мс), машина — потолок множителя (×3 → ×7), дорожка — пол (зоны шире:
44 → 62 px). Толпа отдельно хороша тем, что купленное улучшение не облегчает
игру: зон становится больше, чем успеваешь ловить, а не пойманная роняет
ажиотаж. Доход и риск растут вместе.

**Экономика.** Стартовый выход ≈13 поцелуев, полностью прокачанный ≈43 (×3.2).
Цены по лестнице ×1.8: 45 → 80 → 145, 810 за все три линии, ≈30 выходов.
Тормоз против гринда — «публика устаёт»: суточная убывающая доходность тем же
механизмом, что у золота.

**Отложено.** Позы (на дорожке червь идёт, а не позирует — вернутся как
остановка для долгого кадра), уровень славы (лестница цен делает ту же работу
бесплатно), новые типы зон.

**Техническая честность.** Дорожка, толпа и параллакс — это CSS/SVG. Но одна
правка нужна в ОБЩЕМ рендерере: сейчас походка включается только когда червь
реально идёт к цели по комнате (`opts.wander` + расстояние), а анимация ведётся
от реальной скорости (`state.speedIntensity` от `instSpeed`). Нужен канал «идёт
со скоростью V, никуда не перемещаясь». Несколько строк, но в файле, от
которого зависят все семь грехов. И `rooms.js` для дорожки не годится — там
коробка со стенами и полом; дорожка будет своей сценой, но тоже описанной
данными.

Кода нет, это проектирование: `docs/plan/17-pride.md`, разделы 4–11.

## Правка 50 (2026-08-27): тщеславие — прогрессия придумана, записана в план

Вопрос был двойной: замкнуть ли поцелуи внутри греха и что вообще качать в
игре, где червь стоит столбом, а игрок тапает по кружочкам. Ответы —
`docs/plan/17-pride.md`, разделы 5–9. Кода пока нет, это проектирование.

**Поцелуи — валюта только тщеславия.** Общая валюта уже есть (золото), вторая
общая размывает обе; шаблон замкнутой валюты задан гневом (осколки → жетоны →
снаряжение). Одно исключение и оно тематическое: купленное за поцелуи носится
ВЕЗДЕ, а работает только внутри греха — тщеславие ровно про то, чтобы
купленное видели. Граница: поцелуи не покупают ничего, что меняет числа за
пределами тщеславия.

**Главный вывод по прокачке: качать нечего, пока нет потолка мастерства.**
Сегодня измерение одно — попал или нет, поэтому любое улучшение сводится к
«стало легче», а игра, которую сделали легче, становится скучнее. Значит
сначала СЕРИЯ: попадания подряд поднимают множитель поцелуев, промах
сбрасывает. Шкала греха от серии не зависит — гарантия остаётся гарантией.
Вторая половина связки: поцелуи дают только зоны, вспыхнувшие НА ЧЕРВЕ, — тогда
место зоны перестаёт быть безразличным.

**Три оси, кормящие один цикл:** поза решает, ГДЕ вспыхивают зоны; образ —
СКОЛЬКО из них попадает на червя; публика — КАК ГУСТО они идут; серия —
МНОЖИТЕЛЬ.

* **Поза** — червь позирует, поза выбирается перед выходом и меняет ПРАВИЛА, а
  не числа (анфас, через плечо, хвост трубой, во весь рот). Технически дёшево:
  у рендерера уже есть живые каналы — `setHeadPose`, наклон ушей, изгиб и
  виляние хвоста, открытость и изгиб рта через `setLivePose`. Поза — это набор
  их значений, новой графики не нужно.
* **Образ** — слоты косметики уже заведены как якоря (`WormMarks.SLOTS`:
  голова, шея, тело, хвост). Предметы за поцелуи дают числовые модификаторы.
* **Публика** — ступень плотности потока, которую игрок поднимает САМ, и
  каждая ступень поднимает базовый множитель. Ось важна тем, что улучшение
  здесь не облегчает игру, а делает её труднее и доходнее.

**Слава** — накопленные за всё время поцелуи, отдельно от тратимых. Открывает
то, что не продаётся: позы, слоты, ступени публики, сцены для позирования
(локации уже данными) и новые типы зон — удержание («долгая выдержка») и
цепочка («серия кадров»). Оба типа не облегчают и не усложняют сами по себе:
они добавляют РЕШЕНИЕ, которого сейчас нет.

Порядок работ намеренно начинается не с поз (самое заманчивое), а с серии и
зон на черве: без них остальное не на чем держать.

## Правка 49 (2026-08-27): тщеславие было непроходимо — зона тапа, тайминг, поток

Мини-игра тщеславия не проходилась вообще. Причин три, и все три —
рассогласование между тем, что игрок видит, и тем, что считает код.

**Зона попадания вчетверо меньше маркера.** Кольцо света стартовало радиусом
44 px и вело глаз, а засчитывался тап только в зелёный кружок радиусом 17 px
в центре. По площади — в 6.7 раза меньше видимого. Игрок целился по кольцу и
«мазал», не понимая почему. Теперь зона попадания равна СТАРТОВОМУ размеру
кольца (`TAP_RADIUS = 44`) и за жизнь цели не меняется: кольцо сжимается
только как индикатор остатка времени, поэтому попасть можно и в последний
момент, когда оно уже схлопнулось в точку.

**Зелёный кружок убран.** Он был единственным источником вранья про размер
зоны, а маркер и так есть — кольцо. Вместо кружка мягкое пятно света ровно по
границе зоны: видно, куда бить, и без «мишени», подсказывающей, что бить надо
точно в центр.

**Ускорение тайминга убрано.** Было: каждый удачный тап отнимал 0.1 сек от
окна, 1.5 → 0.5 сек. То есть наказание приходило ровно за успех и ровно
тогда, когда игрок поверил, что разобрался. Полсекунды на «заметить точку в
случайном месте поля, довести палец и попасть в кружок 34 px» — не сложность,
а физический предел. Теперь окно одно на все зоны — 1.5 сек.

**Сложность даёт поток, а не сжатие окна.** Новая зона появляется каждые
0.8 сек, живёт 1.5 сек, поэтому на экране их держится около двух, и тыкать их
можно в любом порядке. Тап засчитывается той зоне, которой осталось жить
меньше всех: если две наложились краями, честнее закрыть ту, что вот-вот
погаснет. Место для новой зоны ищется несколькими бросками — побеждает точка,
самая дальняя от живых зон (иначе два пятна сливаются в одно).

Спавн считается накопителем в rAF-цикле, а не отдельным `setInterval`: тогда
поток замирает вместе с кадрами, когда вкладка уходит в фон, и не выбрасывает
пачку зон при возврате. За кадр появляется не больше одной зоны — пачка из
трёх читается как баг, а не как сложность.

**Проходимость измерена.** `tools/test-pride.js` открывает игру в браузере и
играет ботом с заданной точностью:

| Точность бота | Победа за |
|---|---|
| 100% | ~10 сек |
| 70% | ~21 сек |
| 50% | ~60 сек |

Числа шкалы: попадание +8, промах −5, распад 1.5%/сек. Промах намеренно
дешевле попадания: плохая серия должна тормозить, а не отбирать всё
набранное. Тап мимо всех зон — тоже промах, иначе выгодно барабанить пальцем
по всему полю: зоны большие, слепая дробь закрывала бы их сама.

**Награда.** В конфиг добавлено `pride.parade.win.goldBase: 5` — вдвое меньше
победы в дуэли гнева, потому что и сессия вдвое короче (золото
пропорционально времени действия, `15-progression.md` §10). Мини-игра теперь
шлёт в `meta` свои попадания и промахи — под будущий множитель за точность.
Разбор и два других предложения по награде (множитель, поцелуи как валюта) —
`docs/plan/17-pride.md`, раздел 4.

Заодно: «поцелуй или вспышка» теперь определяется по ТОЧКЕ ТАПА, а не по всей
зоне. Зона стала большая и почти всегда хоть краем задевает силуэт — по зоне
различие выродилось бы в «всегда поцелуй».

## Правка 48 (2026-08-27): автомат — по устройству настоящих, PAR-лист вместо симуляции

Разобрано, как считают настоящие «однорукие бандиты», и `tools/sim-slots.js`
переписан по этой схеме.

**Что нашлось.** Машина Чарльза Фея (1895): три барабана по десять позиций,
тысяча исходов, выигрыш в 26.4% круток, возврат 75.6%; мелкий выигрыш (две
подковы) равен ставке. Патент Инге Телнеса US 4,448,419 (1984, куплен IGT в
1988) — виртуальные барабаны: видимая позиция отображается на большую таблицу
невидимых, и шанс задаёт таблица, а не картинка на барабане. Отсюда огромные
джекпоты при честной с виду картинке. Третий приём — near-miss clustering:
пустые позиции нарочно ставят рядом с джекпотом, чтобы барабан чаще замирал в
шаге от него; в исследованиях игровой зависимости это один из главных крючков.

**Что взяли.** Виртуальные барабаны: у символа есть вес, шанс считается по
весам. Из этого следует главное техническое изменение — 125 сочетаний трёх
барабанов перебираются целиком, поэтому возврат считается ТОЧНО, без
симуляции и без погрешности. Это и есть PAR-лист, внутренний документ
автомата. Симуляция осталась только там, где перебором нельзя: вклад гарантии
против тильта (она зависит от истории круток) и длина серии без выигрыша.

**Что сделали иначе.** Возврат ниже казиношных 85–98% (у нас автомат — сток
золота, а не развлечение за деньги). Гарантия против тильта — в настоящем
автомате её нет и быть не может, там каждая крутка независима. И сознательно
НЕ подкручиваем «почти выигрыши»: веса ровные.

Калькулятор сам подгоняет выплаты множителем под заданный возврат, поэтому
таблица теперь не подбирается руками. Итог:

| Символ | Вес | Комбинация | Шанс | Платит |
|---|---|---|---|---|
| 💀 | 1 | 💀💀💀 | 0.002% | 66 |
| 🪙 | 10 | 🪙🪙🪙 | 1.56% | 13 |
| 🍖 | 10 | 🍖🍖🍖 | 1.56% | 7 |
| 🦴 | 12 | 🦴🦴🦴 | 2.70% | 3 |
| ▫️ | 7 | 🪙🪙· | 14.06% | 1 — возврат ставки |

Возврат **75.0%**, выигрышных круток **27.0%** — против 75.6% и 26.4% у Фея.
Совпадение случайное (числа подбирал калькулятор под наш коридор 60–80%), но
показательное: коридор попал туда же, куда Фей попал опытным путём. Дольше
пяти пустых круток подряд не бывает, одна шкала алчности стоит 2.5 монеты.

**Безденежье — вопрос закрыт по решению пользователя.** Золото дают почти все
грехи, три монеты находятся за минуту, а на длинной дистанции в кошельке
всегда лежит остаток. Особого режима для нищего игрока не делаем; проверка
кошелька в коде нужна только чтобы крутка не уходила в минус. Запасное
лечение, если тупик всё-таки случится, записано в `16-greed-slots.md` §6:
при нулевом кошельке крутка бесплатна, но ничего не платит.

## Правка 47 (2026-08-27): автомат алчности посчитан, таблица выплат подобрана

Продолжение правки 46. `tools/sim-slots.js` доведён до рабочего вида: берёт
таблицу выплат и гарантию против тильта, гоняет два миллиона круток и печатает
возврат, долю выигрышных круток, самую длинную серию без выигрыша и стоимость
одной шкалы алчности.

Подобрана черновая таблица под возврат 75%:

| Комбинация | Шанс | Платит | Вклад |
|---|---|---|---|
| две монеты | 9.0% | 2 | 18% |
| три монеты | 3.0% | 5 | 15% |
| три черепа | 0.4% | 20 | 8% |
| джекпот | 0.05% | 100 | 5% |
| гарантия раз в 6 | в 8.9% круток | 3 | ~27% |

Выигрышных круток 22.6%, дольше пяти круток подряд без выигрыша не бывает,
одна шкала алчности стоит игроку 2.5 монеты. Отдельно стоит запомнить, что
гарантия против тильта даёт четверть всего возврата: это часть баланса, а не
украшение.

**Найдена дыра, которую надо закрыть до кода:** у игрока может не быть монет,
и тогда шкала алчности не закрывается вовсе — потребность, которую нельзя
удовлетворить без денег, ломает тамагочи. Три варианта решения и рекомендация
— в `16-greed-slots.md`, раздел 5.

Заодно: в `CLAUDE.md` описана вторая группа инструментов — балансовые
калькуляторы, и правило «сначала посчитать, потом добавлять контент».
В `02-economy.md` поставлен указатель на действующую модель чисел.

## Правка 46 (2026-08-27): золото — бюджет на игру, алчность как сток

Золото будут давать почти все грехи, и модель дохода дописана под это.

### Четыре правила
1. Золото никогда не покупает силу — иначе появляется вторая линия
   прогрессии и модель уровней перестаёт работать.
2. Суточный бюджет один на игру, а не на грех: семь грехов делят его, а не
   умножают. Отсюда та дыра, что счётчик доходности сейчас считается по ключу
   `грех.режим.исход` — он должен стать общим на день.
3. Золото за действие пропорционально ВРЕМЕНИ действия. Иначе игрок находит
   самый быстрый грех и крутит только его. Одинаковой должна быть награда за
   минуту, а не за действие.
4. Небесполезным действие делает шкала, а не золото: потребность закрывается
   всегда, золото сверху может и не выпасть.

### Автомат алчности посчитан (tools/sim-slots.js)
На задуманных числах — крутка за монету, выплата 3, гарантия выигрыша раз в
шесть круток:

| Выигрышей на 6 круток | Возврат | Одна шкала (10 круток) |
|---|---|---|
| 1.0 | 50% | игрок теряет 5 |
| 2.0 | 100% | в ноль |
| 2.5 | 125% | печатный станок |

Гарантия «раз в шесть» задаёт нижнюю границу возврата 50%, случайные выигрыши
поднимают её — они и есть ручка настройки.

**Возврат выше 100% ломает экономику мгновенно:** круток никто не
ограничивает, и лучшей стратегией в игре становится «крутить вечно». Значит
автомат обязан быть убыточным; рабочий коридор — 60–80%, то есть одна шкала
алчности стоит игроку 2–4 монеты.

И это попадание в тему, а не компромисс: единственный грех, который не
платит, а берёт, — ровно тот, что про жадность.

## Правка 45 (2026-08-27): уровень силы, лестница карт, натяжение узлов

Продолжение правки 44: модель доведена до того вида, в котором по ней можно
строить контент.

### Уровень силы
`УРОВЕНЬ = log(СИЛА / СИЛА_БАЗЫ) / log(1.6)`. Сила умножается, уровень
прибавляется. Шаг ×1.6 выбран не наугад: по измеренной кривой это решающая
разница — на равном уровне 50% побед, на уровень ниже 13%, на уровень выше
87%. Ровно то, что нужно для ворот в контент.

Что чему равно сегодня: голый червь 0, вход в забег 2.9, мини-босс 2.9, босс
забега 5.6, игрок с полным комплектом 5.7. Читается сразу: контента выше
шестого уровня в игре нет вообще.

### Лестница карт
Карта задаётся одним числом — уровнем босса. Уровень входа = уровень босса
минус 2.7: столько игрок добирает усилениями внутри забега, и это измерено, а
не придумано. Шаг между картами — два уровня (×2.6 силы).

Карту при этом не запирают проверкой: боец на два уровня ниже выигрывает у
босса 4% боёв, и игрок увидит нехватку своими глазами, а не прочитает в
требовании.

### Натяжение внутри карты
«Рядовой — процент от босса» мерить надо не от босса, а от игрока в момент
прихода: внутри забега боец растёт. Измерено `tools/sim-rogue.js`:

| Узел | Отношение сил | Доля от босса |
|---|---|---|
| рядовые | ×3.5…×8 | 4–22% |
| мини-босс | ×2.0 | 29% |
| босс | ×1.03 | 100% |

Босс выходит боем ровно на равных — то, ради чего всё и затевалось. Рядовые
работают на износ, а не на вызов. Цель карты — 35–45% дошедших.

### Золото и косметика
Правило: золото никогда не покупает силу, иначе появляется вторая линия
прогрессии и модель уровней перестаёт работать. Суточный потолок уже устроен
верно (доходность падает с числом побед): активный день ≈ 120 золота. Отсюда
цена первой косметики ≈ 300 и лестница ×1.5 — покупка раз в два-три дня.

**Найденная дыра:** счётчик доходности золота считается по ключу
`грех.режим.исход`, то есть у каждого греха свой. Пока золото даёт один гнев,
это незаметно; когда начнут давать все семь, суточный потолок умножится на
семь. Счётчик должен быть общим на день.

## Правка 44 (2026-08-27): формула прогрессии и два калькулятора

Нужна была модель, связывающая сложность, силу, цену и время: «на сколько
процентов сложнее следующий рубеж, на сколько для него усилиться, сколько это
копить». Сделана — и сразу вскрыла три дыры в текущем балансе.

### Мера силы: проверена, а не придумана
`Сила = ЭХП × ДПС`. Гипотезу проверил `tools/measure-power.js`: три бойца с
одинаковой силой, набранной по-разному (вдвое больше здоровья / вдвое больше
урона / поровну), бьются между собой 51 / 50 / 50%. Значит силу правда можно
свести к одному числу.

### Кривая шанса победы — измерена
40 000 схваток на точку. ×1.0 → 50%, ×1.2 → 67%, ×1.4 → 80%, ×1.6 → 87%,
×0.7 → 20%. Отсюда рабочий шаг ступени: **×1.3 по силе**, не ×1.6. «Карта на
60% сложнее» на практике означает 13% побед в старом снаряжении — это стена, а
не рубеж.

### Три диагноза текущей игры
`tools/progression.js` считает силу, цену и время по живому конфигу:

| Ступень | Сила | ×пред | Цена | Часов |
|---|---|---|---|---|
| голый | 13 | — | 0 | 0 |
| 1 | 62 | ×4.69 | 9 | 0.7 |
| 2 | 142 | ×2.28 | 26 | 1.9 |
| 3 | 192 | ×1.35 | 18 | 1.3 |

1. **Ступени неравномерны до абсурда** — первая покупка усиливает впятеро,
   последняя на треть. Причина в масштабе: при уроне 1–3 прибавка «+1» это
   плюс половина ДПС. Числа боя слишком мелкие для кривой; лечится переводом
   всей боевой арифметики в масштаб ×10.
2. **Время идёт скачками и назад** (0.7 → 1.9 → 1.3 часа) вместо плавного
   роста в 1.2–1.7 раза.
3. **Снаряжение не влияет ни на что.** В бою с ботом противник — копия
   игрока: он растёт вместе с ним, и шанс победы всегда 50%. Забег изолирован,
   боя с игроком нет. Вся лестница покупок ведёт в никуда. Лечится лестницей
   противников: ряд соперников с шагом ×1.3 по силе и растущей наградой.

Плюс отдельная дыра: **дневного шлюза нет вообще**, осколки фармятся
бесконечно. Шлюзом должно стать здоровье бойца — оно уже есть, уже тратится в
боях и уже зарастает по метке времени.

Модель, целевые числа и порядок работ — `docs/plan/15-progression.md`.

## Правка 43 (2026-08-27): забег изолирован от лобби, числа подобраны симуляцией

Вторая половина проблемы из правки 42: таблица врагов забега написана под
базового бойца, а игрок приходит вчетверо сильнее. Забег отделён от лобби
целиком — снаряжение и прокачка внутрь не едут.

### Почему изоляция, а не балансировка под снаряжённого
Враги, растущие вместе со снаряжением, обесценивают само снаряжение: как ни
качайся, сложность та же. Плюс каждый новый предмет в магазине потребовал бы
перепроверки всей таблицы — работа, которая не кончается. При изоляции точка
входа перестаёт меняться, и таблица становится верной навсегда.

Экономика от этого не рвётся, а замыкается: забег даёт жетоны → жетоны
покупают снаряжение → снаряжение работает в боях с ботом и с игроком. Своя
постоянная прогрессия у забега тоже есть — способности.

### Числа подобраны машиной, а не на глаз
`tools/sim-rogue.js` прогоняет двадцать тысяч забегов по настоящей таблице
врагов и настоящей формуле урона. Бой — угадайка один из трёх, и «на глаз
тяжеловато» здесь не значит ничего; значит только доля дошедших.

| Боец | Доходят до конца |
|---|---|
| базовый голый (10 хп, 1–3) | 0.2% |
| **стартовый боец забега (20 хп, 3–5)** | **40%** |
| он же с «шестым чувством» | 63% |
| полный комплект (23 хп, 9–11, броня 2/3/1) | 93% |

Голая база оказалась неиграбельной, а полный комплект — прогулкой. Выбран
вход 20/3–5: гибнут в основном на боссе (47% дошедших) и на мини-боссе (10%),
то есть рубеж посередине и стена в конце. Таблица врагов не тронута.

Заодно измерено, чего стоит «шестое чувство»: +23 процентных пункта
проходимости. Дорого — и цену в пять жетонов это подтверждает.

### Мелочи, которые из этого следуют
* Здоровье лобби больше НЕ замораживается на время забега: теперь это разные
  жизни, и червь зарастает, пока игрок ходит по карте.
* У способности появилось поле `modes`: где она работает. У «шестого
  чувства» — бой с ботом и забег; в PvP такая способность невозможна в
  принципе (`13-passives.md`, раздел 4).
* На экране входа в забег написано, с чем идёшь. Изоляция не должна быть
  сюрпризом на первом же бою.

### Записано в план
* `14-money.md` — премиальная валюта: что продаётся (время, воскрешение,
  снаряжение со способностями, косметика), где граница (в PvP премиум не
  продаётся; всё покупаемое добывается и игрой; не продавать выход из
  наказания, которое сами подстроили), чего не продаём никогда.
* `13-passives.md` — уточнено правило снаряжения со способностями: предмет не
  обязан быть слабым, он даёт заметно меньше, чем предмет того же тира и цены
  без способности.

## Правка 42 (2026-08-27): пол урона — броня снижает, но не отменяет

Живой забег показал дыру: игрок, выкупивший магазин, входит с уроном 9–11,
здоровьем 23 и бронёй 2/3/1, и рядовые враги перестают его царапать вовсе.
Считано по каталогу: у первого врага (5 хп, урон 1–3) удар в тело снимал
РОВНО НОЛЬ, у четвёртого (30 хп, 1–3) — тоже ноль. Бьёт, попадает, ничего не
происходит.

Причина в формуле: `max(0, урон − броня)`. При броне, равной верхней границе
урона, противник исчезает как угроза — не «бьёт слабо», а не бьёт совсем.

Теперь дошедший удар снимает минимум единицу (`minHitDamage` в конфиге).
Броня снижает урон, но не отменяет его; отменяет только правильный блок — там
по-прежнему честный ноль и щит вместо цифры.

Это правило, а не подгонка под текущие числа: оно останется верным при любой
будущей броне и любом будущем враге. И оно нужно независимо от того, что мы
решим про снаряжение в забеге, — награда мини-босса «+3 к защите» против
врага с уроном 1–3 давала бы ту же неуязвимость уже внутри забега.

Разбор второй половины проблемы — разъезда масштабов между снаряжённым
игроком и таблицей врагов забега — в `docs/plan/10-wrath-rogue.md`, раздел 8.

## Правка 41 (2026-08-27): силуэты слотов, вкладки, «шестое чувство»

### Силуэт вместо приглушённого значка
Пустой слот показывал своё эмодзи в полсилы, и читалось это как «выключенная
кнопка», а не как «место под шлем». Теперь там контур — простой узнаваемый
силуэт того, что в слот встаёт: купол каски, жилет, варежка, клинок, щит.
Контуры лежат данными в каталоге (`WRATH_GEAR.slots[].shape`), а не в коде, и
тот же силуэт стоит на вкладке магазина: один язык на оба экрана.

### Вкладки вместо длинного списка
Прилавок разбит по слотам: вкладка на каждый, под силуэтом дробь «куплено из
всего». Видно, где ещё есть что взять, не открывая вкладку. Выкупленная
вкладка гаснет, но остаётся на месте — пропадающие вкладки заставляли бы
переучивать порядок.

Меню прокачки разбито на две: числа бойца и способности.

### Пассивка «шестое чувство»
Первая способность, меняющая правило боя, а не число. В начале раунда она
помечает одну из своих зон, по которой противник точно не ударит: выбор из
трёх превращается в выбор из двух, шанс закрыться растёт с 33% до 50%. Цена —
пять жетонов, то есть пятнадцать побед; это самая дорогая покупка в игре.

Помеченная зона не запирается: закрыться ею можно, это просто плохой ход.
Запрет отнял бы у игрока право ошибиться.

Устроена как ветка прокачки с одним уровнем и без числовой прибавки, поэтому
в состоянии ничего нового не появилось. Купленная уходит из меню, как и всё
купленное, — и её значок показывается в панели бойца в лобби: иначе игрок
нигде не увидел бы, что способность у него есть.

### Что ради этого изменилось в бою
Противник бросал свои зоны В МОМЕНТ удара. Пока намерений никто не видит, это
равнозначно броску в начале раунда, но показать можно только то, что уже
решено. Теперь план противника бросается в начале раунда, до выбора игрока, и
до конца раунда не меняется.

Это честнее и само по себе — план не подстраивается под выбор игрока, — и
открывает дорогу замаху, чтению привычек и показу чужого выбора после раунда.

Проверено тестом: за тридцать раундов помеченная зона ни разу не совпала с
зоной удара, отметка в разметке всегда стояла на нужной зоне.

Разбор и планы на другие способности — `docs/plan/13-passives.md`.

## Правка 40 (2026-08-27): панель бойца в лобби, чистый прилавок, заготовка разблокировки

### Счётчик зарастания убран
Он показывал, сколько секунд осталось до полного здоровья, — и был вреден
дважды. Во-первых, это лишнее: число здоровья растёт на глазах, по нему всё
видно. Во-вторых, из-за него строка характеристик меняла ширину каждую
секунду, и от этого дёргалась вся раскладка лобби. Теперь раз в секунду
меняются ровно два значения: ширина полосы и число на ней.

### Панель бойца вместо строки в углу
Мелкая строка «❤️ 🗡 🛡» сверху убрана. Над червём — там, где было много
пустого места, — встала настоящая панель: широкая полоса здоровья во всю
ширину и под ней итоги колонкой, значки слева, числа справа.

Показываются ТОЛЬКО итоги: сколько всего здоровья, какой разброс удара с
учётом всего надетого и прокачанного, сколько брони. Разбивка «база плюс
предмет плюс прокачка» наверх не выносится — иначе выходит стена цифр.
Вместо этого прибавка от каждого предмета написана в его собственном слоте:
надел перчатки — в ячейке перчаток стоит «🗡 +1». Итог наверху, слагаемые
там, где лежат сами вещи.

Заодно исчез знак зоны из трёх фигур, который я завёл правкой 38. Слот и
есть зона: шлем — это голова, броня — это тело. Объяснять очевидное
пиктограммой не нужно.

### Отказ вместо всплывающего сообщения
Тап по запертому бою больше не поднимает подсказку. Вздрагивает и краснеет
сама панель здоровья — тот же язык, что в магазине, где вздрагивает валюта,
которой не хватило. Всплывающее сообщение из лобби убрано совсем.

### Прилавок показывает только то, что можно взять
Купленное с прилавка уходит. Смотреть на витрину с тем, что уже своё, незачем
— где оно и что даёт, видно в лобби, в своём слоте. Осталась дробь у группы:
сколько из скольких уже куплено.

Выкачанная до потолка ветка прокачки так же уходит из списка.

### Недоступное не притворяется кнопкой
Раньше у предмета не по карману была нормальная на вид кнопка «купить», и
только нажатие показывало, что денег нет. Теперь строка приглушена и обведена
пунктиром, цена не выглядит кнопкой — но нажимается вся строка целиком, и тап
по ней всё равно отвечает: вздрагивает та валюта, которой не хватило. Игрок
узнаёт причину, не нажимая на то, что притворялось доступным.

### Заготовка под разблокировку
У предмета и у ветки прокачки может стоять условие появления:

```js
unlock: { counter: 'wrath.duel.fights', at: 10 }
```

Пока оно не выполнено, вещь не показывается ВООБЩЕ — не серым, не силуэтом,
никак. Смысл ровно в этом: игрок проводит десятый бой и обнаруживает в
магазине то, чего там вчера не было. Показанная заранее запертая вещь такого
не даёт, она просто дразнит.

Условие читается из накопительных счётчиков, то есть из того, что игрок уже
делал. Ни у одной вещи условия пока не стоит: это каркас (`Backend.isUnlocked`),
проверенный тестом — предмет с условием исчезает с прилавка и возвращается,
когда условие снято.

## Правка 39 (2026-08-27): экран боя — диагональ, равные мишени, подсказка по опыту

### Раскладка
Бойцы разведены по углам сильнее: противник в верхний правый, игрок в нижний
левый, между ними свободная полоса. Кинжалы встали ровно в центр этой полосы.
Общая шапка со здоровьем разобрана — полоска каждого бойца переехала к нему,
в свободный угол напротив: у верхнего слева, у нижнего справа. Помнить, какая
полоска чья, больше не нужно, а центр экрана освободился целиком.

Бойцам от этого досталось больше места: коробка выросла, множитель вписывания
поднят с 0.82 до 0.94.

### Мишени одного размера
Зона повторяла габариты части тела, и голова выходила вдвое крупнее хвоста —
то есть выбор зоны решался меткостью, а не тактикой. Теперь три круга одного
размера (`ZONE_TOUCH`), от тела берётся только место.

### Подсказка, которая затихает
Задача была двойная: новичку видно, куда жать, опытному не мигает всю драку.
Решено двумя независимыми механизмами.

**По ходу.** Дышит только та сторона, от которой ждут выбора: не выбрана
защита — свой червь, выбрана — противник, выбрано всё — сами кинжалы. Даже в
полный голос это не ёлка из шести кругов, потому что горит всегда одна
сторона.

**По опыту.** Пульс задан числом вдохов, и оно зависит от накопительного
счётчика боёв: до пяти боёв — бесконечно, до пятнадцати — шесть вдохов,
дальше — два в начале шага. Счётчик накопительный, а не сессионный: это про
опыт игрока. Вся громкость — три CSS-правила с разным
`animation-iteration-count`, никакой логики в кадрах.

Заодно: выбранная зона остаётся яркой, а две другие на той же стороне уходят
в тень — выбор сделан, напоминать больше нечего.

### Проверка
Тест меряет прямоугольники бойцов, полосок здоровья и кинжалов и требует,
чтобы они не пересекались, а также читает вычисленные стили зон на каждом
шаге и при разном опыте. «Наложилось» и «мигает не там» — это то, что глазами
ловится последним, а числом первым.

## Правка 38 (2026-08-26): гнев без единого слова

Первый шаг инварианта 9 (`docs/plan/11-no-words.md`): в игре не должно быть
букв. Пилотом взят гнев — там их было больше всего, и там сразу всё: числа,
цены, характеристики, итоги, вопрос при выходе.

### Чем заменились слова
* **«уровень 2 из 3»** — точками: залито столько, сколько куплено.
* **«не хватает жетонов»** — сама валюта в кошельке дёргается и краснеет.
  Игрок видит, ЧЕГО не хватило, вместо того чтобы читать об этом.
* **«голова / тело / хвост»** — знаком из трёх фигур (`WrathFighter.zoneMark`):
  кружок, прямоугольник, треугольник, нужная горит. Один знак на лобби,
  магазин и карточку предмета.
* **«надето / куплено»** — галочка и галочка в зелёном кружке.
* **«Снять»** — пустая строка в списке слота. Снять значит выбрать «ничего»,
  и весь список читается одним правилом: выбери, что тут стоит.
* **«ПОБЕДА! / ПОРАЖЕНИЕ...»** — 🏆 / 💀 / 🤝 во весь экран, «Окей» — ✓.
* **«броня!»** над бойцом — щит.
* **«золота 50% — много побед за сутки»** — 🪙 и 50%. Правило не
  объясняется, показывается величина; игрок выведет его за два-три боя.
* **Имена противников, предметов и усилений** ушли с экрана, но остались в
  данных: они нужны журналу и будущему серверу.

### Общее окно — заодно и для остальных шести
Название греха в шапке снято: значок и цвет рамки уже говорят, где игрок.
Вопрос при выходе — значок греха, под ним шкала, которая на глазах утекает в
ноль, и два ответа: ↩ и ✕. Уход помечен тем же крестиком, который только что
нажали, поэтому вопрос читается как «точно этот крестик?». Окно общее, так
что бессловесным вопрос стал сразу во всех семи грехах.

### Чем это проверяется
Не глазами. Сценарий обходит все экраны гнева в headless-браузере и ищет
буквы в ЖИВОМ DOM — на лобби, в карточке слота, в магазине (включая отказ),
в прокачке, на карте забега, в бою, в вопросе при выходе и на итоге боя.
Смотреть исходники бесполезно: слово может приехать из конфига, из шаблона,
откуда угодно. Сейчас находок ноль.

## Правка 37 (2026-08-26): рогалик гнева — карта, шесть противников, награды

### Забег стал картой, а не списком
Узлы стоят точками на извилистой дорожке, игрок жмёт на следующую доступную.
Координаты точек лежат в конфиге вместе с самим узлом, поэтому подвинуть узел
— это правка одного числа. Дорожка проводится через них кубическими кривыми с
вертикальным изгибом: линия выходит из точки вниз и входит в следующую
сверху, а не режет расстояние хордой.

Каждый отрезок дорожки — свой путь: так видно, докуда игрок дошёл и каким
путём, и не надо считать длину линии. Толщина линии держится `vector-effect="non-scaling-stroke"`:
viewBox растянут по коробке (`preserveAspectRatio="none"`), и обычная обводка
растянулась бы вместе с координатами.

### Развилка — выбор одного пути из трёх
Первая версия расставила магазин, привал и событие последовательными узлами —
это была ошибка чтения плана. Развилка расходится натрое (магазин / привал /
событие), игрок проходит ОДИН путь, и дорога сходится обратно в мини-босса.
Перед боссом — вторая такая же.

Отсюда карта из ШАГОВ, а не из точек: обычный шаг рисуется одной точкой,
развилка — тремя, и отрезки дорожки строятся от каждой точки шага к каждой
точке следующего. Каким путём прошли, хранится номером (`chosen`): по нему
красным светится ровно та ветка, которой шли, а не все три.

### Закрытые пути вместо отсутствующих
Магазин и случайное событие ещё не сделаны. Они всё равно нарисованы на
развилке — серыми и ненажимаемыми, — а привал работает, поэтому развилка
из-за них не становится тупиком. Убрать их было бы хуже: тогда при их
появлении карта поедет, а игрок уже привык к другой.

### Числа согласованы и лежат данными
Шесть противников с именами, здоровьем и разбросом урона; награда за победу
привязана к противнику, а не к узлу; карточки усилений (+2 к атаке, +5/+10 к
здоровью, +3 к защите) — тоже таблицей. Копилки золота, которую предлагал
первый набросок, больше нет: монеты даёт только босс.

Усиление здоровьем поднимает И максимум, И текущее. Иначе на полном здоровье
его брали бы вслепую «на потом», а побитый не брал бы никогда.

### Бой не переписан, а параметризован
`wrath-duel.js` получил необязательный «заказ»: противник от узла, здоровье
забега, бонусы усилений и два колбэка. Без заказа это ровно тот же обычный
бой, что был. Исход засчитывается СРАЗУ по концу боя, а не по кнопке «Окей»:
здоровье уже записано в забег, и закрытая между ними игра не должна стирать
бой.

### Что вылезло при проверке
**Конец карты считался не тем числом.** Когда счёт узлов для показа стал
«проходимые, без закрытых» (8 вместо 11), этим же числом остался ограничен и
индекс текущего узла — забег объявлял себя пройденным на восьмом индексе, то
есть за два боя до босса. Один и тот же `total` использовался для двух разных
вещей. Теперь конец карты считается по её длине, а игроку показывается счёт
проходимых узлов.

**Здоровье лобби размораживалось посреди забега.** И лобби, и уход с боя
честно звали `resumeHeal()` — а через оба этих места игрок проходит по
несколько раз за забег. Правило переехало внутрь `Backend.resumeHeal()`: пока
в состоянии есть забег, разморозки не будет, и помнить об этом каждому
вызывающему больше не нужно.

Разбор и порядок работ — `docs/plan/10-wrath-rogue.md`.

## Правка 36 (2026-08-26): кольцо удержания над головой, чистка лишних слов

### Индикатор удержания стал кольцом над головой
Был полоской под червём — и там же стояла строка-подсказка «удерживай червя»,
которая эту полоску и перекрывала. Строка убрана совсем, индикатор переехал
на макушку и заполняется по кругу.

Заполнение — открывающийся пунктир: обводка нарисована штрихом длиной ровно в
одну окружность, а анимируется сдвиг штриха. Одно свойство, никакой
перерисовки по кадрам.

Место кольца считается по РЕАЛЬНЫМ габаритам головы и живёт внутри сцены
персонажа: у подросшего червя голова крупнее и выше — кольцо переедет вместе с
ней и отмасштабируется вместе с телом.

**Позиция считается в момент касания, а не при монтировании.** Первая версия
ставила кольцо сразу после `fitWorm()` и промахивалась: перестановка червя
доезжает до экрана только следующим кадром рендерера, и голова, измеренная
сразу после, оказывается ещё на старом месте. Тот же вывод, что в правке 17,
только с другой стороны: мерить надо не раньше, чем нарисовано.

Отдельно стоит запомнить, как это выглядело при отладке: кольцо оказалось в
левом нижнем углу, а `position` у него был `static`. Правила `.hold-ring`
просто не попали в файл — при вырезании блока со строкой-подсказкой в тот же
диапазон попал только что вставленный блок кольца. Симптом «элемент стоит не
там» стоит начинать проверять с вопроса «а применились ли его стили вообще».

### Слова, которых не должно было быть
* **Строка «удерживай червя»** — убрана. Она объясняла словами то, что
  показывает сам жест, и занимала место ровно там, где рисуется индикатор.
* **Приписка в пустом слоте** «предметы появятся в магазине гнева и в
  рогалике» — убрана. Она была ещё и неверной: снаряжение гнева в рогалике не
  выпадает, оттуда выносятся только ресурсы. А где брать предметы, видно в том
  же лобби отдельной строкой магазина.

### Повторный тап по слоту закрывает карточку
Раньше карточку инвентаря можно было закрыть, только попав в пустое место
мимо всего кликабельного, — а на плотном экране такого места почти нет. Теперь
тап по другому слоту переключает карточку, тап по тому же — закрывает.

### Проверено
Браузером: кольцо стоит над макушкой (центр выше верхней границы головы),
за 700 мс заполняется на 44%, отпустил — исчезло и всплыла подсказка, полное
удержание открывает прокачку. Карточка слота: шлем → броня → броня (закрылась)
→ броня (открылась снова). В пустом слоте только название и «Слот пуст».
Все семь мини-игр открываются без ошибок. Вживую на телефоне не смотрели.

## Правка 35 (2026-08-26): боевая прокачка по удержанию, шрамы меняются на жетон

### Прокачка открывается жестом по самому червю
Полторы секунды пальцем на персонаже в лобби — открывается меню боевой
прокачки. Жест выбран не ради экзотики: качают самого червя, и «нажать на
него» — самое понятное действие. Удержание, а не тап, потому что тап здесь
слишком дёшев: по персонажу промахиваются, гладят его, тыкают от нечего
делать.

**Главная задача была не в жесте, а в том, чтобы о нём узнали.** Скрытый
жест, о котором нигде не сказано, не существует. Решено двумя способами
сразу:

* строка под червём: «✊ удерживай червя — боевая прокачка»;
* **жест показывает себя**. От первого касания под червём появляется полоска
  и начинает заполняться за те же полторы секунды. Отпустил раньше — полоска
  исчезла, но игрок уже увидел, что что-то набиралось, и следом всплыла
  подсказка «держи палец на черве». То есть обычный тап, который раньше
  просто ничего не делал, теперь сам обучает жесту.

### Что качается
Три ветки по три ступени, за жетоны: базовый урон (+1/+2/+3), базовое
здоровье (+2/+4/+7), скорость зарастания (1.5/2/3 ХП в секунду). Числа — в
`ECONOMY.minigames.wrath.upgrades`.

`bonus` в конфиге — СУММАРНАЯ прибавка на уровне, а не приращение. Так
уровень читается одним числом, без сложения по цепочке, и вставить ступень
посередине можно, не пересчитывая остальные.

**Прокачка отличается от снаряжения тем, что не снимается.** Предмет можно
снять, купленный уровень остаётся навсегда — поэтому и экран отдельный от
магазина, и открывается он жестом по персонажу, а не кнопкой в списке
режимов. В расчёте бойца прокачка складывается с предметами: те же три
числа, просто одно слагаемое надето, а другое вросло.

### Обмен шрамов переехал в это же меню
Был на тапе по червю — стал кнопкой «15 🩹 → 🎟 1». Две причины: тап слишком
дёшев для необратимого действия (пятнадцать шрамов не вернуть), и он занимал
собой единственное «нажатие на персонажа», которое понадобилось прокачке.
Кнопка в меню решает обе: чтобы до неё добраться, надо осознанно открыть
экран, а внутри уже видно и цену, и остаток.

Заодно исправлено расхождение с планом: пятнадцать шрамов дают **жетон**, а
не осколок, как было в предыдущей правке.

### Мелочь про тест, а не про игру
Проверка «слепые тапы после боя ничего не переключают» начала падать. Код
оказался ни при чём: тест успевал дотянуть до момента, когда кнопка «Окей»
уже проявилась, и честно нажимал на неё. Отдельным прогоном подтверждено, что
до 1,1 секунды экран не реагирует ни на один тап, а тест сдвинут ближе к
концу боя.

### Проверено
Браузером: короткое нажатие оставляет в лобби и показывает подсказку, полоска
за 500 мс заполняется на треть; полное удержание открывает прокачку; покупка
урона поднимает бросок с 1–3 до 2–4, покупка зарастания — скорость с 1 до 1.5
ХП/сек, жетоны списываются; обмен шрамов кнопкой убирает 15 штук и даёт жетон.
Все семь мини-игр открываются без ошибок, бой и магазин работают как раньше.
Вживую на телефоне не смотрели.

## Правка 34 (2026-08-26): конец боя без случайных тапов, зарастание только вне боя

### Результат боя больше не смахнуть пальцем
Раньше оверлей с исходом появлялся сразу с кнопками, а палец в этот момент
почти всегда уже летит к экрану: добивающий удар только что тапнули. Один
лишний тап — и результат исчез, не прочитанный.

Теперь бой кончается так: оверлей накрывает поле и **секунду не принимает
нажатий вообще** (кнопки на нём ещё нет, а сам он гасит тапы), потом плавно
проявляется единственная кнопка «Окей» — возврат в лобби.

Кнопка «Ещё раз» убрана намеренно. После боя здоровье не полное, и решение
«драться ли снова» принимается там, где видно, сколько его осталось и сколько
зарастать, — в лобби. Заодно исчез экран ожидания внутри боя: он был нужен
ровно для того, чтобы «Ещё раз» не запускал мгновенное второе поражение.

### Зарастание идёт не везде
Здоровье зарастает в комнате, в лобби и при закрытой игре — но не в бою: там
оно ресурс этой самой драки. Поэтому вход в бой замораживает зарастание
(`Backend.freezeHeal()`), а возврат в лобби размораживает.

**Флаг заморозки лежит в состоянии, а не в памяти экрана.** Иначе достаточно
было бы закрыть игру посреди боя и вернуться через час к целому червю. По той
же причине разморозка висит не только на выходе из боя, но и на входе в
лобби: из боя выходят и закрытием приложения, и тогда снимать заморозку
будет некому.

Рогалик, когда появится, заморозит зарастание на весь забег — там своё
лечение (`03-wrath.md`).

Заодно упростилось чтение: `GameState.fighterHpRaw()` отдаёт накопленное без
потолка, а потолок (он зависит от снаряжения) применяется при чтении. Так
заморозке не нужно знать про предметы, и расчёт максимума не продублировался
в ядре.

### Проверено
Браузером: в лобби заморозки нет, в бою есть; здоровье 4 в бою остаётся 4
и через три секунды; сразу после результата кнопка скрыта, `pointer-events`
у неё `none`, два тапа по экрану ничего не переключают; через секунду кнопка
проявляется и уводит в лобби, где заморозка снята и здоровье снова растёт.
Все семь мини-игр открываются без ошибок, обмен шрамов и магазин работают
как раньше. Вживую на телефоне не смотрели.

## Правка 33 (2026-08-26): здоровье между боями и обмен шрамов

### Здоровье бойца стало постоянным
Раньше каждый бой начинался с полного здоровья, и получить урон ничего не
стоило: проиграл — сразу дерёшься снова, целый. Теперь здоровье живёт между
боями и зарастает само, единица в секунду.

Считается тем же способом, что и шкалы грехов: в состоянии лежит
`fighter: { hp, updated_at }`, а «сколько сейчас» выводится формулой при
обращении (`GameState.fighterHp`). Тикающего таймера нет, поэтому зарастание
идёт и при закрытой игре — как и требует первый инвариант проекта.

Записывается здоровье **после каждого раунда**, а не в конце боя. Причина
простая: мобильная сессия обрывается посреди чего угодно, и свернувший игру
на трёх ХП должен вернуться к трём ХП, а не к полным.

**Ноль — это ожидание, а не отказ.** Пока червь без сил, бой не начинается,
но и лобби, и экран результата пишут, сколько секунд осталось, а кнопка
оживает сама. Без этого «Ещё раз» после поражения означало бы мгновенное
второе поражение — худший из возможных ответов на нажатие.

Побочный смысл: у боя появилась цена. До этого драться можно было
бесконечно подряд, и единственным ограничением была убывающая доходность
золота. Теперь поражение стоит паузы, а `hp` у предметов впервые означает не
только «дольше живёшь в бою», но и «быстрее возвращаешься».

### Обмен шрамов — тапом по червю
Тап по червю в лобби гнева. Мало шрамов — всплывает сообщение «Шрамов мало:
7 из 15» и само гаснет. Хватает — вопрос «Обменять 15 шрамов на 🩸 1?» с
двумя кнопками.

Два разных ответа на один и тот же тап — это решение, а не непоследовательность:

* сообщение без кнопок там, где ничего не произошло и решать нечего. Диалог
  с «ок» на пустом месте — наказание за любопытство: игрок всего лишь тапнул
  по своему червю;
* вопрос там, где есть что терять. Шрамы не вернуть, и молча стереть
  пятнадцать штук нельзя.

Уходят самые старые шрамы: тело зарастает в том же порядке, в каком его
били, и свежие следы последнего боя остаются на месте. Случайный выбор
читался бы как «исчезло непонятно что».

Под червём появилась строка «🩹 шрамы: 7 из 15 до обмена» — она же подсказка
про тап. Без неё обмен остался бы невидимой функцией: по червю никто не
тапает просто так.

**Расхождение с планом зафиксировано.** В `03-wrath.md` за 15 шрамов обещан
жетон, дан осколок — втрое меньше. Вернуть план — одна строка в конфиге.

### Мелочи, которые пришлось решить
* **Часы зарастания.** Число здоровья пересчитывается раз в секунду, пока
  экран открыт (`WrathFighter.startHealClock`). Это не таймер состояния:
  здоровье растёт формулой само по себе, часы только перерисовывают число —
  ровно как единственный таймер в `main.js`, перерисовывающий шкалы.
* **Тап ловит коробка червя, а не SVG.** Тело узкое и извилистое, попасть по
  нему пальцем труднее, чем по области, где червь стоит. В этой области всё
  равно больше ничего нет.
* **Воскрешение поднимает и бойца.** `Backend.revive()` сбрасывает здоровье к
  полному: воскрешение — это второй шанс целиком, а не «встал, но избитый».

### Проверено
Браузером: здоровье 3 → через 4 секунды 7 → через час упирается в максимум;
бой стартует с 4/10, а не с полного; после раунда в состоянии лежит ровно то
же число, что в бою; при нуле экран пишет «ЧЕРВЬ БЕЗ СИЛ» и «зарастает: 10 с»,
кнопка сама оживает и запускает бой с полным здоровьем. Обмен: 17 шрамов →
осталось 2, начислен осколок, тело перерисовалось сразу и в лобби, и на
главном экране. Все семь мини-игр открываются без ошибок. Вживую на телефоне
не смотрели.

## Правка 32 (2026-08-26): жетоны гнева и магазин

### Что сделано
Экономика гнева из плана (`docs/plan/03-wrath.md`) собрана целиком для
существующего режима — боя с ботом, — и к ней сделан магазин.

**Жетоны.** Победа даёт осколок, три осколка складываются в жетон
автоматически. Кнопки «обменять» нет намеренно: курс фиксированный, выбора у
игрока никакого, а лишний экран есть. Осколок при этом остаётся видимой
единицей прогресса — в лобби и в магазине он показан дробью «2/3 до жетона»,
то есть сразу читается, сколько побед до покупки.

**Осколок за каждые три поражения.** Счётчик накопительный, а не суточный:
три поражения за неделю тоже обязаны сложиться в осколок. Для этого в
состоянии появились `counters` — счётчики за всё время, рядом с суточными
`daily_counters`, которые чистятся через неделю.

**Золото с убывающей доходностью.** 1–5 побед за сутки — полное золото,
6–15 — половина, дальше десятая часть. Запрета играть при полной шкале нет:
режется только внешняя валюта, шкала и осколки капают всегда полностью.
Проверено прогоном двадцати побед подряд: 12 → 12 → 12 → 12 → 12 → 6 …
6 → 1 → 1.

**Магазин гнева** — третий экран внутри того же окна: снаряжение по слотам за
жетоны, сгруппированное по слотам, с ценой и состоянием («куплено», «надето»).
Недоступное по деньгам не прячется, а гасится: по нему игрок и понимает,
ради чего копить. У каждого слота написано «куплено 1 из 3» — потолок
объявлен заранее, как требует план. Покупка в пустой слот сразу надевается:
покупка и есть намерение пользоваться.

**Строка награды в конце боя.** Раньше мини-игра не знала, что ей начислили:
она бросала событие с исходом, а ответ Backend уходил в main.js. Теперь
main.js пересылает начисленное обратно событием `minigame:awarded`, и бой
показывает «+1 🩸 · +12 🪙 · 🎟 собрался жетон!». Правило при этом не
нарушено: мини-игра по-прежнему ничего не решает и не считает — она
показывает пришедший ответ.

### Решения, которые стоит помнить
* **Размен не пишется в список заработанного.** Сначала он уменьшал осколки
  в `awarded.currencies` и после победы на экране получалось «−2 осколка».
  Игрок заработал осколок, а жетон — это то, во что осколки сложились;
  показывать вычет как награду — прямая ложь. Теперь размен отдельным полем
  `exchanged`.
* **Цена предмета — карта валют, а не число.** `{ wrath_token: 3 }` вместо
  `3`, чтобы завтра предмет мог стоить золота или кристалла без правки кода.
* **Ступени предметов (`tier`) — в каталоге.** Магазин считает «сколько
  всего» сам, поэтому новый предмет не требует правки счётчика.
* **Кнопка debug сменилась.** Была «Снаряга» и выдавала все предметы —
  теперь «Жетоны» и выдаёт валюту: у предметов появился честный источник, и
  дублировать его отладочной выдачей значит проверять не то, что играется.

### Проверено
Прогоном в браузере: 20 побед и 4 поражения через `Backend.minigameResult` —
пороги золота, сборка жетонов, осколок на каждом третьем поражении, записи в
журнале (`ledger`) по каждому начислению и размену. Покупка без денег даёт
понятный отказ, покупка с деньгами списывает жетоны, кладёт предмет в
инвентарь и надевает его в пустой слот. Вживую на телефоне не смотрели.

### Не сделано
Обмен шрамов на жетон (15 штук = 1 жетон) — источник жетонов есть в плане, но
он про экран обмена с превью «до/после», это отдельная работа. Рогалик и PvP
по-прежнему с замком. Уровни и апгрейды предметов, косметические эффекты
удара, пересборка — следующие слои магазина.

## Правка 31 (2026-08-25): гнев на общей модели — лобби, снаряжение, бой

### Что сделано
Гнев перестал быть «мини-игрой бой» и стал грехом с экранами: лобби →
режим → бой. Решение и вводные — `docs/plan/09-wrath-rework.md`.

* **Свой рисунок червя удалён.** Была функция `drawWormFigure()` на 145
  строк, рисовавшая на canvas другое существо: другие пропорции, свой цвет,
  никакой связи с состоянием игрока. Теперь и в лобби, и в бою стоит общая
  модель (`WormRenderer`) — со шрамами, взрослением и косметикой.
* **Противник** — второй экземпляр той же модели, зеркальный (`flip: true`),
  приходит через `Backend.getOpponent()`. Пока это копия игрока с пометкой
  `is_self_copy`; форма ответа уже серверная.
* **Лобби:** червь, пять слотов снаряжения вокруг него, строка
  характеристик, список режимов (готов один, остальные с замком).
  Слоты строятся из `src/config/wrath-gear.js` — новый слот добавляется
  записью в конфиге, без правки разметки.
* **Снаряжение** работает в лоб: оружие +урон, броня −урон по своей зоне,
  ХП от предметов. Хранится только надетое (`state.equipment`),
  характеристики выводятся из него каждый раз.
* **Скрещённые кинжалы** вместо двух палочек. Логика прежняя: выбрал зону —
  кинжал загорелся своим цветом, оба горят — тап проводит раунд.
* **Механика боя не менялась** — сознательно оставленная угадайка один из
  трёх.

### Ошибка в рендерере, которую нашёл зеркальный противник
Зоны ударов ставятся по реальному телу: `getPartPoint()` даёт точку части.
У игрока они встали правильно, у противника уехали за пределы сцены — точка
головы оказывалась на x=410 при сцене шириной 300.

Причина в `worm-renderer.js`: пересчёт из системы части в систему сцены был
написан как `E × W⁻¹` вместо `W⁻¹ × E`. Матрицы перемножались в обратном
порядке — и это не всплывало годами, потому что обе матрицы были
ПЕРЕНОСАМИ, а переносы переставимы. Первый же зеркальный персонаж добавил
`scale(-1,1)`, которое с переносом не переставляется, и ошибка проявилась
целиком.

Вывод на будущее: код, который «всегда работал», может быть неверным и
просто не встречать входных данных, на которых это видно.

### Габариты частей: getBBox вместо getBoundingClientRect
Первая версия мерила части клиентским прямоугольником, и мишени выходили
заметно больше самих частей. Две причины: клиентский прямоугольник раздут
фильтрами (тень, размытие), и он в ЭКРАННЫХ пикселях — а сцена масштабирована
дважды, своей трансформацией и общей трансформацией холста. `getBBox` +
матрица сразу даёт геометрию в единицах сцены. Помощник общий
(`WrathFighter.boxOf`), им же вписывается сам червь.

### Раскладка: бойцы по диагонали, а не в строку
Силуэт червя почти квадратный (≈180×210), арена — узкая и высокая
(366×700). Двое в строку занимают полосу 2:1, оставляя сверху и снизу две
трети пустоты, и увеличить их нельзя: по ширине они уже упираются друг в
друга. По диагонали (противник в верхнем углу, игрок в нижнем) каждому
достаётся вдвое больший масштаб, пустота исчезает, и читается это как
глубина.

### Кадры: комната под окном продолжала анимироваться
Замер на процессоре ×4 медленнее: в бою 41 fps. Под открытым окном
мини-игры продолжал крутиться персонаж главного экрана — то есть в бою жили
ТРИ червя. Проверка «сцену не видно» в рендерере такое не ловит: сцена
видима, её просто накрыли непрозрачным окном.

| сценарий (процессор ×4) | было | стало |
|---|---|---|
| комната | 60 | 60 |
| лобби гнева | 50 | 60 |
| бой (два бойца) | 41 | 52 |
| после закрытия | 59 | 60 |

Лечится паузой: у рендерера появился `setPaused()`, у общего окна —
`pauseRoom()` / `resumeRoom()`. Гасить комнату на входе в мини-игру теперь
может любая игра на общем окне, не только гнев.

### Проверено
Headless-браузером на 390×844: лобби открывается, слоты надеваются и
снимаются, характеристики пересчитываются (10 ХП пустым, 13 с кастрюлей и
шкурой), бой идёт до конца, победа начисляет осколок и заполняет шкалу,
зона последнего пропущенного удара уезжает в результат (по ней садится
шрам). Переходы лобби↔бой гасят своих персонажей: после пяти кругов
открыть-закрыть в документе остаётся ровно один SVG — главного экрана.
Все семь мини-игр по-прежнему открываются и закрываются без ошибок.
Вживую на телефоне не смотрели.

### Не сделано (сознательно)
Магазин, рогалик, PvP — по плану. Источника предметов для игрока пока нет:
они выдаются кнопкой «Снаряга» в debug-режиме, потому что проверять слоты и
характеристики надо уже сейчас, а обещать в конфиге предметы, которых негде
взять, — нет.

## Правка 30 (2026-08-25): одно окно на все мини-игры, начиная с гнева

### Задача
Окна мини-игр писались по одному и разошлись: у зависти крестик в правом
верхнем углу и вопрос при выходе, у гнева крестик посередине между полосками
здоровья и выход молча, у лени и алчности свои карточки своих размеров.
Игрок видит не семь игр, а одну игру с семью экранами — и каждый раз ищет,
где закрывать и спросят ли его.

### Что сделано
Общее окно вынесено в ядро: `src/core/minigame-window.css` (вид) и
`src/core/minigame-window.js` (сборка). Гнев переведён на него первым,
подробности правил — в `docs/minigame-window.md`.

Одинаково у всех: экран целиком, рамка с отступом от края цветом греха,
шапка с названием, крестик в правом верхнем углу, вопрос «Выйти из
мини-игры? Прогресс не сохранится.».

**Почему сборкой, а не соглашением о классах.** Соглашение уже было — и
именно его нарушили семь раз. Скопировать разметку «как у соседа» дешевле,
чем сделать правильно, поэтому правильное должно происходить само:
`MinigameWindow.attach()` переносит содержимое экрана внутрь рамки и сам
навешивает крестик и вопрос. Забыть крестик или переписать текст вопроса
своими словами больше нельзя.

Цвет рамки берётся из `ECONOMY.sins[грех].color` — оттуда же, откуда
красится кружок греха в HUD. Отдельного списка цветов окон нет.

### Что вылезло сразу: обрезанные морды
Окно гнева было карточкой 400×700, стало почти во весь холст (366×~790).
Масштаб бойца в `drawWormFigure()` считался только от высоты
(`scale = h / 300 * 1.65`), а позиция — от `h * 0.25`. На высоком и узком
холсте фигуры выросли шире холста, и головы обрезало рамкой.

Починено правильно, а не подбором коэффициента: габарит фигуры описан в
условных единицах (рог 45 над центром головы, 19 на сегмент, 22 на раструб
хвоста, половина ширины — голова радиусом 32), масштаб — меньшее из «влезть
по высоте» и «влезть по ширине», по вертикали фигура центрируется. На старом
размере карточки формула даёт тот же масштаб, что и раньше (3.09 против
3.08) — то есть это не перерисовка, а снятие зависимости от формы окна.

Бойцы после этого занимают по половине арены без наездов друг на друга:
раньше их растаскивали отрицательными полями (`margin-left: -14%`), и это
работало ровно потому, что фигура была уже своего холста. Теперь вылезающий
за край холст означает обрезанную морду.

**Общий вывод, годный для остальных шести игр:** окно у всех станет одного
размера и почти во весь экран, поэтому любое «считается от размера холста»
надо проверять на вписывание по ОБЕИМ сторонам. Раскладка, подобранная под
свою карточку, при переезде поедет.

### Заодно
* Из гнева убраны своя карточка `.wrath-modal`, свой крестик и своё
  затемнение с `backdrop-filter` — тем самым в открытом виде исчезло
  полноэкранное размытие (то самое, на котором в правке 29 утекал фпс в
  закрытом).
* Крестик уехал в шапку окна, и вместе с ним ушёл спор по `z-index` между
  `.fighting-hud` и оверлеем результата: полоски здоровья стали обычной
  строкой в колонке, а не позиционированным слоем со своим контекстом
  наложения.
* Возврат HUD после закрытия — `MinigameWindow.restoreHud()` вместо семи
  скопированных строк в каждой игре.

### Проверено
Headless-браузером на 390×844: окно открывается, крестик посреди боя
показывает вопрос, «Остаться» возвращает в бой, «Выйти» закрывает; после
конца боя крестик закрывает без вопроса; повторное открытие не собирает
рамку второй раз и не оставляет висеть вопрос; ошибок в консоли нет.
Вживую на телефоне не смотрели.

### Не сделано (сознательно)
Боевая система гнева не тронута: в плане (`docs/plan/03-wrath.md`) она
переделывается целиком — пять зон удара против двух блоков, снаряжение по
слотам, рогалик. Это отдельная работа, и мешать её с переездом окна значит
не понять потом, что именно сломалось. Остальные шесть игр тоже остались на
своих окнах — переводить их разом означает шесть непроверенных раскладок в
одном коммите.

## Правка 29 (2026-08-25): утечка фпс после мини-игр

### Жалоба
«После запуска и выключения нескольких мини-игр начинает люто утекать фпс».

### Замер
Стенд: открыть и закрыть все семь мини-игр, померить кадры. Воспроизвелось
с первого круга и обратно не поднималось.

| сценарий | было | стало |
|----------|------|-------|
| старт, ни одной игры не открывали | 59.8 | 59.9 |
| после 1 круга по всем семи | 40.2 | 60.2 |
| после 5 кругов | 39.6 | 59.9 |
| то же на процессоре ×4 медленнее, после круга | 33.9 | 59.9 |
| при ОТКРЫТОЙ мини-игре, процессор ×4 | 35.7 | 57.5 |

Причин оказалось две, и обе — «выключили, но не до конца».

### 1. Закрытый экран мини-игры продолжал отрисовываться (главная)
`#wrath-game` и все остальные в закрытом виде — это `opacity: 0`, но
по-прежнему `display: flex`. Прозрачность не отменяет отрисовку. У каждого
экрана на весь экран висит `backdrop-filter: blur(8px)` — и композитор
честно размывает под ним фон каждый кадр. Семь полноэкранных размытий
поверх живого SVG персонажа.

Проверено по одному, снимая по одному свойству у закрытых экранов:

| что снять у закрытых экранов | fps |
|------------------------------|-----|
| ничего (как было) | 41 |
| только `backdrop-filter` | 60.1 |
| остановить ВСЕ анимации внутри | 39.9 |
| `visibility: hidden` | 60.2 |
| `content-visibility: hidden` | 59.8 |
| `display: none` | 59.5 |

То есть вся потеря сидела на `backdrop-filter`, а анимации внутри
(`zone-spin`, `bulb-idle`, `cloud-drift` и прочие) не стоили ничего.

Лечение — `visibility: hidden` на `.minigame-screen:not(.active)` в общем
`style.css`: лучший замер, поддержан везде, и заодно снимает всё остальное —
тени канвасов, `drop-shadow`, покраску. Переход `visibility` с задержкой
0.35 с, чтобы не срезать затухание при закрытии; при открытии — без
задержки. `!important` обязателен: у каждой мини-игры свой `transition` на
`#id`, он специфичнее любого правила по классу.

### 2. Мини-игра оставляла свой экземпляр персонажа крутиться вечно
«Тщеславие» и «Чревоугодие» монтируют СВОЙ `WormRenderer` и намеренно
держат его между открытиями, чтобы не платить за пересборку семисот узлов.
Но `close()` не останавливал его цикл: невидимый червь продолжал
анимироваться, сотнями `setAttribute` в кадр. Один забытый экземпляр — минус
около трёх кадров навсегда, и по одному на каждую такую мини-игру.

Починено в самом рендерере, а не в мини-играх: `tick()` теперь проверяет,
на экране ли холст, и на скрытом сразу выходит. Проверка стоит запроса
раскладки, поэтому пока холст виден — раз в полсекунды, а пока скрыт —
каждый кадр (кадр всё равно пустой, зато возврат к жизни мгновенный;
задержка тут недопустима — мини-игра сразу после `open()` меряет реальный
bbox персонажа).

### Проверка
Пять кругов по всем семи с настоящими тапами внутри и закрытием крестиком:
60 fps держится, число узлов не растёт (2210 → 2213), тап по полу после
всего по-прежнему уводит червя, ошибок в консоли нет. Скриншоты всех семи
на ВТОРОМ открытии — регрессий нет; кадр посреди затухания подтверждает,
что `visibility` не срезает анимацию закрытия.

### Вывод на будущее
**Закрытый полноэкранный слой обязан быть НЕВИДИМЫМ, а не прозрачным.**
`opacity: 0` — это «нарисуй и не показывай»: браузер по-прежнему платит за
размытие, тени и покраску. И **тот, кто крутит цикл, тот его и
останавливает**: требование «не забыть позвать `destroy()`» уже забыли
дважды, поэтому следит теперь сам рендерер.

## Статус на 2026-08-16 (правка 24): Лень — полная переработка генерации растения по структурной модели (PlantModel) ✅

## Правка 24 (2026-08-16): Лень — растение не "пропадает" за границей, стебель растёт из земли, листья не с одной стороны, инструменты — сцена, а не иконки в воздухе

### Фидбек пользователя (после первой переработки Лени)
Первая переработка (fullscreen-окно, богаче сцена сада, объёмный стебель)
была отклонена: (1) растение пропадает за какой-то странной границей на
скриншотах — жёсткий вертикальный шов через холм и растение; (2) стебель
при наклоне выглядит "обрубленным"/приклеенным поверх сцены, крутится
прямо в горшке, острый низ, не читается как "растёт"; (3) листья растут
все с одной стороны, некрасивый паттерн. Явный запрос: полностью
переработать генерацию через структурную модель (по аналогии с
worm-model.js/worm-renderer.js) — типы стебля, типы листьев, крепление к
земле, наследование трейтов листьями от стебля, антиклип-расстановка.
Плюс отдельно: иконки инструментов сада должны быть не "интерфейсом в
воздухе", а частью композиции сцены (стеллаж/натюрморт с глубиной).

### 1. Причина "растение пропадает за границей" — не CSS-обрезка, а обрезка ПИКСЕЛЯМИ canvas
`#plant-canvas` был `width:100%; height:100%` от `.pot-wrap`, а `.pot-wrap`
был узким — всего 42% ширины сцены (столько же, сколько сам керамический
горшок). При сильном наклоне/изгибе стебля часть кривой уходила за
пиксельные границы САМОГО canvas (не CSS-контейнера с overflow:hidden, а
именно растрового холста) — контент за пределами `0..canvas.width`
просто не рисуется, без ошибок и без визуальной "обрезки рамкой", отсюда
и ощущение "растение куда-то пропадает" на резкой прямой линии (край
canvas).

Фикс — двухуровневый:
1. `.pot-wrap` (сцена роста, содержит canvas) расширена с 42% до 78%
   ширины сцены. Сам керамический горшок (`.pot-visual`) сделан УЖЕ
   `.pot-wrap` и отцентрован (`width:54%; left:23%`, пересчитано так,
   чтобы АБСОЛЮТНАЯ ширина горшка на экране осталась прежней — иначе
   горшок снова превратился бы в "вазу", как уже было в прошлой сессии).
2. Жёсткий программный клэмп координат в новой функции `stemPointAt()`
   (см. ниже) — `x` каждой точки стебля клэмпится в `[marginX, w -
   marginX]`. Это гарантия, а не просто "стало реже" — тест прогнан на
   4 размерах вьюпорта (320×640 .. 480×1000) и на форсированных
   экстремальных трейтах (макс. наклон + макс. curve на каждом из 4
   типов стебля) — 0 случаев выхода за границы во всех случаях.

### 2. Причина "стебель обрубленный/приклеенный" — наклон применялся сразу от самой земли
Раньше кривая стебля (одна квадратичная Безье) начиналась СРАЗУ под
углом наклона `p.tilt` от точки основания — то есть даже нижний,
прилегающий к почве участок стебля был уже наклонён. Нижний "срез"
силуэта (перпендикуляр к касательной в t=0) поэтому получался под
углом к горизонтали круглой почвы в горшке — визуально читалось как
"стебель обрублен и положен сверху", а не "вырос из земли".

Фикс: стебель теперь строится кубической кривой Безье с 4 контрольными
точками, из которых вторая (`P1`) стоит СТРОГО НАД основанием
(`P1 = {x: baseX, y: baseY - rootLen}`, `rootLen = stemLen * 0.22`) —
это гарантирует, что касательная строго вертикальна в самой нижней
части стебля независимо от общего наклона/изгиба, которые начинают
проявляться только выше. Плюс добавлено "корневое расширение"
(`drawRootFlare()`) — мягкое радиальное пятно + 2-4 нерегулярных
бугорка (трейты `p.rootFlare`, `p.rootBumps`) у самого основания,
чтобы граница стебель/почва не читалась как ровный обрубленный овал, а
плавно "уходила" в темноту горшка.

### 3. Причина "все листья с одной стороны" — крепление листьев считалось по ДРУГОЙ кривой, чем сам стебель
Самое важное найденное расхождение: сам стебель рисовался по
квадратичной кривой (`quadPoint` от base/mid/tip), а точка крепления
каждого листа считалась ЛИНЕЙНОЙ интерполяцией между base и tip —
т.е. по прямой линии, не по фактической изогнутой кривой стебля. На
изогнутых/наклонных стеблях прямая и кривая заметно расходятся, из-за
чего листья визуально "съезжали" в одну сторону от настоящего
нарисованного стебля — при этом сторона (`side = i%2===0?1:-1`)
математически честно чередовалась, просто относительно НЕПРАВИЛЬНОЙ
линии. Отсюда и жалоба "паттерн какой-то странный", несмотря на то что
код формально альтернировал стороны.

Фикс: заведена ОДНА функция `stemPointAt(geo, t)`, которая используется
и для сэмплирования силуэта стебля (в цикле по `SAMPLES`), И для
позиции каждого листа (`leaf.t` из плана `PlantModel.buildLeafPlan()`),
и для позиции цветка (`t=1`) — единственный источник правды о том, где
проходит стебель. Дополнительно у функции есть нормаль `(nx, ny)` в
каждой точке — от неё же теперь считается и угол крепления листа
(`Math.atan2(ny, nx) + leaf.angleVar`), и точка выноса черешка листа НА
поверхность стебля (`origin = sp + normal*halfWidth*0.55*distScale`,
а не в его центр) — лист явно "растёт из" поверхности стебля, не
проваливается в его толщу.

### 4. Структурная модель растения — новый файл `plant-model.js`
По аналогии с `claude/worm-model.js`/`claude/worm-renderer.js` заведён
отдельный файл `plant-model.js` (в проекте — `plant-model.js`, на диске
`/src/minigames/sloth/plant-model.js`, подключается в index.html ДО
`sloth.js`). `PlantModel` не знает о размерах canvas — только
безразмерные трейты и планы, вся геометрия в пикселях по-прежнему
считается в `sloth.js` (`getStemGeometry`/`stemPointAt`, аналог
worm-renderer.js для этой модели). Содержит:
- 4 типа стебля (`STEM_TYPES`): `straight`/`arching`/`zigzag`/`spiral` —
  каждый со своим множителем изгиба (`curveMul`) и, для zigzag/spiral,
  волновым узором поверх основной кривой (`wobbleAmp`/`wobbleFreq`,
  амплитуда растёт от 0 у корня к максимуму у верхушки — волна не
  портит вертикальный старт).
- 4 формы листа (`LEAF_SHAPES`): добавлена `fern` (перистый) к прежним
  oval/pointed/heart.
- 3 типа листорасположения (`LEAF_ARRANGEMENTS`): `alternate` (не строго
  через один — сторона переключается с вероятностью ~62% за шаг, не
  чаще 1 повтора подряд, чтобы не выглядело механически),
  `opposite` (пары на одной высоте по разные стороны), `whorled`
  (мутовки по 3).
- Явно определено "крепление к земле": `rootFlare` (во сколько раз шире
  расширяется основание) + `rootBumps` (2-4 нерегулярных бугорка).
- Расширен разброс трейтов: hue 30-280 (было 60-200), отдельные
  `satBase`/`lightBase` (не единая формула на всех), thickness 0.5-2.4
  (было 0.6-2.0), leafCount 4-11 (было 3-8).
- `buildLeafPlan()` считает t-позиции листьев вдоль стебля (0.16-0.90)
  С РАВНОМЕРНЫМ шагом + джиттер, ограниченный ПОЛОВИНОЙ шага между
  листьями — гарантия отсутствия наложения по высоте при любом
  количестве. Каждый лист получает `hueShift` (небольшое отклонение от
  hue стебля — "наследует" цвет с вариацией) и `sizeScale`/`distScale`.
- Прогрессивное появление: лист становится виден, когда
  `this.growth` переваливает за `leaf.t * 0.78` (снизу вверх, как
  растёт по-настоящему), с локальным плавным ростом размера
  (`localGrowth`), а не все сразу по общему проценту.

### 5. Инструменты сада — от плоского ряда к композиции на полке
Было: `.tools-row` (flex, `justify-content:space-around`) — 4
одинаковых иконки на равном расстоянии, весь ряд читался как оверлей
интерфейса. Стало: `.tools-shelf` — небольшая деревянная полка
(`::after`, наклонённый градиентный брусок) с 4 предметами разного
размера/наклона/глубины (z-index), разложенными как натюрморт: лейка
крупнее и по центру, пакетик с семенами слева повыше, мешок
удобрений справа как бы прислонён к лейке, тяпка ещё правее/выше как
прислонённая — по явному примеру пользователя.

**Важный найденный подводный камень**: наклон каждого предмета сначала
попытался быть на `.tool-slot` (позиционирующем родителе перетаскиваемого
`.tool`) — но по спецификации CSS элемент с `transform` создаёт новый
containing block для ПОТОМКОВ с `position:fixed`. `.tool.dragging`
переключается именно на `position:fixed` (чтобы лететь за пальцем по
всему экрану через `el.style.left/top` в координатах viewport) — если
бы `.tool-slot` остался с `transform:rotate()`, `fixed`-потомок
позиционировался бы относительно ПОВЁРНУТОГО слота, а не viewport, и
`clientX`/`clientY` из обработчика перестали бы совпадать с реальным
экраном (предмет улетал бы не туда, куда тянет палец). Фикс: наклон
перенесён на сам `.tool` (`#tool-seed`/`#tool-can`/`#tool-fert`/
`#tool-rake`, применяется только в состоянии покоя), а `.tool.dragging`
явно сбрасывает `transform: none` — во время переноса предмет "берут в
руки" ровно, наклон был частью того, как он лежит на полке, а не
частью самого предмета.

**Второй найденный подводный камень**: изначально `.tools-shelf` была
достаточно высокой (top:5%, height:34%) и пересекалась с зоной, где
растёт полностью выросший стебель (центр сцены, верхняя треть) —
деревянный брусок полки визуально "резал" стебель поперёк на
скриншотах, что могло читаться как новый баг обрезки. Исправлено
геометрически: `.tools-shelf` подвинута строго в самый верх сцены
(`top:3%; height:15%`, т.е. полностью укладывается в 3-18% высоты
сцены) — это ВЫШЕ максимально возможной верхушки полностью выросшего
растения (~21-22% от верха сцены при `heightFrac=1`, посчитано от
`topPadding` в `getStemGeometry()`), то есть полка и растение
геометрически гарантированно не пересекаются, а не "обычно не
пересекаются".

### Проверка
Headless Playwright-тест (`test/verify.js`, `test/verify-viewports.js`):
- 60 случайных растений на `growth=1`: 0 случаев выхода за границы
  canvas (стебель + все листья), 0 случаев "все листья на одной
  стороне" (при leafCount≥4), 0 случаев отрицательного зазора между
  соседними листьями по высоте; встречены все 4 типа стебля и все 3
  типа листорасположения.
- Форсированные экстремальные трейты (макс. наклон ±28° + макс. curve
  на каждом из 4 типов стебля, включая zigzag с максимальной волной):
  0 отказов.
- 4 вьюпорта (320×640, 390×900, 430×700, 480×1000) — по 15 случайных
  растений на каждом, 0 выходов за границы на любом из них.
- Полный шаговый прогон (drag семя → полить → удобрить → тяпка убирает
  сорняки → плод появляется на верхушке → сбор плода → победный оверлей)
  — воспроизведён через синтетические `mouse.move/down/move/up` на
  реальных DOM-элементах, без ошибок в консоли (`pageerror` слушатель
  пуст на всём прогоне).
- Скриншоты на каждом шаге и на разных вьюпортах — растение видимо
  растёт из горшка без обрыва, листья заметно чередуются по сторонам,
  полка с инструментами не пересекается со стеблём ни на одном кадре.

### Открытый вопрос
Живого теста от пользователя на реальном устройстве после этой правки
ещё не было — ждём фидбек. Числовые константы (rootLen=22% от длины
стебля, lateralMax=40% ширины canvas, позиция/размер полки) подобраны
расчётом и headless-измерением, не глазами на живом экране — возможна
тонкая доп. подстройка после реального просмотра.

## Файлы (актуальные версии сохранены в проекте)
- `plant-model.js` — новый файл (правка 24): структурная модель
  растения для мини-игры Лень (типы стебля, типы/план листьев,
  крепление к земле). Аналог `worm-model.js`, но для этой мини-игры.
- `sloth.js` — правка 24: генерация трейтов делегирована в
  `PlantModel.generate()`; геометрия стебля переведена с квадратичной
  на кубическую кривую с вертикальным корневым участком
  (`getStemGeometry`); заведена единая `stemPointAt()` (аналог
  worm-renderer.js — единственный источник точек стебля, используется
  и для силуэта, и для листьев, и для цветка); добавлены
  `drawRootFlare()`, `widthAt()`, форма листа `fern`.
- `sloth.css` — правка 24: `.pot-wrap` расширен 42%→78% (антиклип для
  canvas), `.pot-visual` пересчитан на 54%/left:23% (сохранить
  абсolютный размер горшка); `.tools-row` заменён на `.tools-shelf`
  (полка-композиция, наклон предметов на `.tool`, не на `.tool-slot`
  — см. подробности выше); `.tool.dragging` сбрасывает `transform`.
- `index.html` — правка 24: разметка `.tools-row`→`.tools-shelf`
  (2 места — основной блок и сохранённый в проекте тестовый слепок),
  добавлен `<script src="src/minigames/sloth/plant-model.js">` перед
  `sloth.js`; версии `sloth.css?v=3→v=4`, `sloth.js?v=5→v=6`.

## Важный вывод на будущее (закрепить в подходе, дополняет выводы правок 1-17 ниже)
**Вывод правки 24 (canvas ≠ CSS-контейнер)**: когда визуальный элемент
рисуется на `<canvas>`, а не в DOM/SVG, "обрезка" может происходить на
уровне САМОГО РАСТРА (`canvas.width`/`canvas.height`), а не через
`overflow:hidden` CSS-родителя — эти два вида обрезки выглядят на
скриншоте почти одинаково (резкая прямая граница), но чинятся по-разному
(CSS-обрезка — правкой `overflow`/размеров контейнера; canvas-обрезка —
либо увеличением самого canvas, либо клэмпом координат в JS). Если
элемент "пропадает за границей" на canvas — проверять оба варианта.

**Вывод правки 24 (общий источник геометрии)**: если несколько
визуальных элементов должны быть "прикреплены" к одной и той же кривой
/ фигуре (здесь — листья к стеблю), они ОБЯЗАНЫ считать позицию через
ту же самую функцию, что рисует саму кривую — не через
"приблизительно похожую" отдельную формулу (здесь: линейная
интерполяция вместо квадратичной кривой). Даже когда сопутствующая
логика (чередование стороны) математически верна, расхождение
базовой геометрии полностью ломает визуальный результат.

**Вывод правки 24 (transform на предке ломает position:fixed)**: если
элемент при определённых обстоятельствах переключается на
`position:fixed` (drag-механика, модалки, оверлеи), ни один из его
CSS-предков не должен иметь `transform` (даже decorative
`rotate()`/`scale()`) — по спецификации это создаёт новый containing
block, и `fixed`-потомок начинает позиционироваться относительно
предка, а не viewport, ломая любые расчёты, завязанные на
`clientX`/`clientY`. Наклон/decorative-transform в таком UI-дереве
переносить на сам перетаскиваемый элемент (и явно сбрасывать
`transform:none` в состоянии `.dragging`), а не на его контейнер.

## Статус на 2026-08-16 (правка 23): Тщеславие — второй источник того же "призрак"-бага (winOverlay после победы) ✅

## Правка 23 (2026-08-16): close() не снимал 'show' с winOverlay — призрак-баг возвращался после ПОБЕДЫ в Тщеславии

### Уточнение от пользователя
Правка 22 (см. ниже) не решила проблему полностью: баг с перехватом
кликов в Алчности воспроизводился ТОЛЬКО если перед этим Тщеславие было
пройдено ДО КОНЦА (шкала 100%), а не просто открыто/потыкано/закрыто
досрочно.

### Причина
`finishGame()` при победе добавляет классу `#pride-win-overlay` `show`
(в CSS: `.pride-win-overlay.show { opacity:1; pointer-events:auto; }`).
Снимался этот класс только в `resetAll()` — а она вызывается лишь из
`open()`, то есть при СЛЕДУЮЩЕМ заходе в игру. `close()` этого не делал:
```js
close() {
    this.stopLoop();
    this.clearActiveTarget();
    if (this.screenElement) this.screenElement.classList.remove('active');
},
```
Если игрок побеждал и сразу закрывал мини-игру (не открывая Тщеславие
снова), `#pride-win-overlay` оставался с классом `show` навсегда. Его
родитель `#pride-game` в этот момент невидим (`opacity:0` по снятию
`.active`), но **opacity не блокирует клики** у потомка с собственным
`pointer-events:auto` — ровно тот же механизм, что и в правке 22, просто
на другом элементе. В итоге почти на весь экран (размер `.pride-modal`)
оставался невидимый кликабельный слой, перехватывающий тапы в других
мини-играх (Алчность и т.д.) — именно поэтому баг проявлялся только
после ПОБЕДЫ, а не после обычного досрочного закрытия.

### Фикс
`close()` теперь дублирует ту же очистку, что и `resetAll()`:
```js
close() {
    this.stopLoop();
    this.clearActiveTarget();
    if (this.screenElement) this.screenElement.classList.remove('active');
    if (this.winOverlay) this.winOverlay.classList.remove('show');
    if (this.fieldGlowEl) this.fieldGlowEl.className = 'pride-field-glow';
},
```

### Проверка остальных toggle-классов в pride.js
Прогнал по всем местам `classList.add/remove`/`className =` — остальные
(`pride-target`, `pride-ring`, `flash-green/red`, `pride-fx-*`) либо
всегда `pointer-events:none` в CSS, либо элементы создаются/удаляются
из DOM целиком (не персистентные) — повторов этого паттерна не найдено.

`index.html`: `pride.js` версия `?v=5` → `?v=6`.

## Статус на 2026-08-16 (правка 22): Тщеславие — починен "призрак"-баг, ломавший другие мини-игры ✅

## Правка 22 (2026-08-16): pointer-events на SVG персонажа Тщеславия был не сколирован — ломал клики в других мини-играх

### Баг
Пользователь сообщил: после игры в "Тщеславие" и перехода в "Алчность"
(слоты) — тап по автомату переставал что-либо делать, а тап по крестику
закрытия почему-то дёргал рычаг. Несколько тапов по крестику сначала
крутили барабаны, потом закрывали игру.

### Причина
В правке 19 (силуэтный хит-тест поцелуя) в `pride.css` был добавлен:
```css
.pride-worm-stage .worm-stage-svg {
    pointer-events: auto;
}
```
Это правило было БЕЗУСЛОВНЫМ — не скопировано под `#pride-game.active`.
А `#pride-game` не удаляется из DOM при закрытии (только `opacity:0;
pointer-events:none` на контейнере) — и `z-index: 999999 !important`
общий у всех мини-игр. `pointer-events:auto` на дочернем SVG
переопределяет унаследованный `none` от родителя ВСЕГДА, даже когда
Тщеславие закрыто. В итоге поверх всего экрана (включая другие
мини-игры) оставался невидимый, но кликабельный силуэт Свиночервя —
"призрак", перехватывающий тапы, которые должны были доходить до
слотов (а попадание по крестику Алчности, который лежит физически
выше в разметке/z-index в своём модальном окне, иногда всё же
проходило мимо призрака и попадало на автомат под ним — отсюда
"крестик дёргает рычаг").

### Фикс
```css
#pride-game.active .pride-worm-stage .worm-stage-svg {
    pointer-events: auto;
}
```
Теперь `auto` включается только пока Тщеславие реально открыто, и
пропадает вместе с классом `.active` при закрытии — как и весь
остальной интерактив мини-игры.

### Проверка остальных мини-игр
Прогнал `pride.css` построчно (`grep -n "pointer-events"`) — все
остальные вхождения либо изначально `none` (декоративные/оверлейные
слои), либо `auto` только под классами-состояниями, которые JS
корректно снимает при закрытии (`.show`, и т.д.) — повторов бага не
найдено. Дополнительно сверил `greed.css`, `gluttony.css`, `wrath.css`,
`lust.css`, `envy.css`, `sloth.css` — ни в одном из них нет
персистентно смонтированного элемента с безусловным
`pointer-events:auto` вне scoped-класса активности; уязвимых мест по
этому же паттерну не обнаружено.

`index.html`: `pride.css` версия `?v=5` → `?v=6` (JS не менялся,
`pride.js` версию не трогаю).

## Статус на 2026-08-16 (правка 21): Тщеславие — ускоряющийся таймер кольца + починен схлопнувшийся размер окна ✅

## Правка 21 (2026-08-16): ускорение таймера + починка схлопнувшейся модалки

Два независимых изменения за один заход.

### Ускоряющийся таймер кольца
По просьбе пользователя: первый этап — 1.5 сек (как и было), но каждый
УДАЧНЫЙ тап (любой из двух видов — поцелуй или вспышка) ускоряет окно
СЛЕДУЮЩЕГО тайминга на 0.1 сек; промах темп не меняет; пол — 0.5 сек,
дальше игра не ускоряется (иначе стала бы физически невозможной).
Реализовано через новое поле состояния `PrideMinigame.currentRingMs`
(стартует с `PRIDE_CONFIG.RING_SHRINK_START_MS=1500`, уменьшается в
`resolveTarget()` на удачном тапе на `RING_SHRINK_STEP_MS=100`, зажато
снизу `RING_SHRINK_MIN_MS=500`, сбрасывается в `resetAll()` при каждом
новом заходе в мини-игру) — используется в `spawnTarget()` вместо старой
константы `RING_SHRINK_MS` и для CSS-transition кольца, и для `setTimeout`
таймаута.

### Модалка схлопывалась в тонкую полоску
Скриншот показал: после перехода на "почти fullscreen" размер окна
(правка 19) в браузере пользователя (локальный WebDAV-просмотрщик, судя по
всему нестандартный/встроенный вебвью, не обычная мобильная Safari)
модалка схлопнулась до размера контента — виден только заголовок и пустая
шкала, а само поле с постаментом и персонажем — нулевой высоты (flex:1
у `.pride-field-frame` не от чего было растягивать, раз у родителя не
было настоящей высоты).

Причина — `.pride-modal { width:90vw; height:90vh; }`: юниты `vw`/`vh`,
судя по всему, в этом конкретном вебвью посчитались некорректно (это
известный класс проблем у встроенных/нестандартных вебвью, в отличие от
обычных мобильных браузеров). При этом сам `#pride-game` (затемнение на
весь экран, `position:fixed; inset:0`) отрисовался корректно на весь
экран — значит, `%`-размеры от уже гарантированно растянутого на весь
экран предка работают надёжно, а вот `vh`/`vw` от вьюпорта — нет.

Исправление: `.pride-modal` больше не задаёт размер через
`width:90vw/height:90vh`. Вместо этого — `position:absolute; top:5%;
left:5%; right:5%; bottom:5%;` внутри `#pride-game` (который сам теперь
`position:fixed; inset:0`, вместо прежнего flex-center — центрирование
через inset уже не нужно, сам отступ 5% со всех сторон и центрирует, и
масштабирует). Тот же самый результат ("на 10% меньше, с теми же
пропорциями"), но проценты считаются от размера предка, а не от вьюпорта
напрямую — надёжнее в разных браузерах/вебвью.

### Проверка
Живого повторного теста ещё не было — ждём подтверждения, что модалка
теперь растягивается на весь экран (за вычетом 5% отступа со всех сторон)
и в этом браузере тоже, раз именно он — единственная среда тестирования
у пользователя.

## Статус на 2026-08-16 (правка 19): Тщеславие — поле на весь экран, убрана белая рамка, исправлен баг определения "поцелуй/вспышка", индикация зелёная/красная ✅

## Правка 19 (2026-08-16): фидбек по первому живому тесту Тщеславия

Скриншот живого теста показал: (1) окно мини-игры — маленькая квадратная
карточка, слишком тесная; (2) внутри рамки поля была лишняя тонкая белая
окантовка (её приняли за отдельный декоративный элемент, хотя задумывалась
она как область цветовой вспышки-индикации); (3) баг — АБСОЛЮТНО ВСЕ
удачные тапы засчитывались как "поцелуй", независимо от того, где реально
появлялась цель; (4) розовая вспышка окантовки на удачном "поцелуе"
визуально читалась как ошибка, а не похвала.

### 1. Окно мини-игры — почти на весь экран
Раньше `.pride-modal`/`.pride-field-wrap` были маленькой квадратной
карточкой (270px). Пользователь явно попросил: тот же формат/пропорции, что
и весь игровой экран, просто уменьшенный примерно на 10% — не квадрат.
`.pride-modal` теперь `width:90vw; height:90vh` (обе стороны в vw/vh —
пропорция автоматически повторяет пропорцию экрана устройства, просто в
0.9 масштабе). Побочный эффект оказался желательным: персонаж рисуется
ФИКСИРОВАННЫМИ px-размерами (радиусы сегментов и т.п. не зависят от
контейнера — см. worm-renderer.js), поэтому на маленьком квадратном поле он
выглядел непропорционально стиснутым; на поле почти в размер экрана он
теперь того же визуального масштаба, что и на главном экране — то есть уже
"нормального" вида, без отдельной подгонки размера модели под мини-игру.

### 2. Убрана белая внутренняя рамка
`.pride-field-inner-frame` (белый слой между коричневой окантовкой и
тан-полем) убран целиком. Осталась только `.pride-field-frame` (коричневая
окантовка, padding создаёт толщину рамки) → `.pride-field` (тан-фон) —
без промежуточного белого слоя. Область цветовой вспышки-индикации — это
`.pride-field-glow` (inset box-shadow на самом `.pride-field`), она никуда
не делась, просто раньше выглядела похоже на лишнюю рамку, когда не горела.

### 3. Баг "все попадания — поцелуй": прямоугольный bbox вместо силуэта
Причина: `isOnWorm()` проверял, попадает ли позиция цели в прямоугольный
`getBoundingClientRect()` группы `.worm-root` (голова+тело+хвост целиком).
Этот прямоугольник почти всегда покрывает БОЛЬШУЮ часть поля (включая
пустые промежутки внутри силуэта — там, где визуально персонажа нет, но
bbox всё равно его "занимает"), особенно пока поле было маленьким. Из-за
этого практически любая случайная точка внутри поля попадала в этот
прямоугольник — отсюда и баг.

Исправление: `isOnWorm()` больше не мерит прямоугольник. Вместо этого — для
центра цели и 8 точек по её периметру (радиус цели, каждые 45°) вызывается
`document.elementFromPoint()` и проверяется, попал ли реально отрисованный
элемент персонажа (`.closest('.worm-root')`) — то есть буквально то, что
физически нарисовано на экране (форма тела, ушей, хвоста), а не
прямоугольная оценка. Чтобы `elementFromPoint()` вообще "видел" персонажа (а
не проваливался сквозь него к полю под ним), контейнеру SVG персонажа
явно включён `pointer-events: auto` (см. `.pride-worm-stage .worm-stage-svg`
в pride.css) — раньше там стоял `none` (был нужен, чтобы клики не
"терялись" на отдельных частях SVG, а всегда доходили до обработчика на
`#pride-field`; но события всплывают вверх по DOM в любом случае, поэтому
включение pointer-events на SVG никак не ломает обработку тапов).
Проверка нескольких точек по периметру (не только центра цели) — по
явной просьбе: "даже если target слегка касается силуэта — это поцелуй".

### 4. Индикация — только зелёный/красный
Розовая вспышка окантовки убрана: три цвета лишние, розовый визуально
читался как "ошибка". Теперь `flashField()` вызывается только с 'green'
(любой удачный тап) или 'red' (промах/пропуск). Разница
поцелуй/вспышка-фотоаппарата осталась — но выражается только ЛОКАЛЬНЫМ
эффектом на месте тапа (💋 или нарисованный SVG-блик), не цветом рамки.

### 5. Раскладка персонажа+постамента — теперь по вертикали ТОЖЕ по центру
Раньше `layoutStage()` мерил только положение постамента по CSS (`bottom:
6%`) и подгонял персонажа под него. Теперь считается по-другому и точнее:
меряется реальная высота отрисованного силуэта (`bodyRect.height`) и реальная
высота уже отрисованного постамента (`pedestalRect.height`, у постамента
фиксированный px в CSS), суммарная высота композиции центрируется по
вертикали внутри поля, и уже ОТСЮДА вычисляется, на какой высоте должны
стоять и персонаж (`setPosition`), и сам постамент (`pedestalEl.style.top`,
теперь выставляется JS, а не фиксированным CSS-процентом). Это устраняет
проблему "персонаж должен целиком помещаться и красиво стоять по центру" —
работает при любом размере поля и (в будущем) при любом размере персонажа
после взросления, без правки чисел.

### Проверка
Живого браузера по-прежнему нет за пределами того, что прислал пользователь
на скриншоте — код перечитан на предмет согласованности (id/классы между
pride.js и новой разметкой, z-index слоёв, что `pointer-events` на SVG не
ломает всплытие тапов до `#pride-field`). Ждём следующий живой тест.

## Статус на 2026-08-16 (правка 18): Тщеславие — полностью новая механика (тап по цели с кольцом-таймером) вместо шахмат/карт/костей ✅

## Архитектура (см. также claude/worm-model-architecture.md)
- `claude/worm-model.js` — данные модели, localStorage, `mergeWormOverride`.
- `claude/worm-renderer.js` — единственный рендерер SVG для персонажа.
- `worm.js` (root) — тонкий глю для главного экрана.
- `gluttony.js` (root) — мини-игра Чревоугодие.
- `src/minigames/pride/pride.js` — мини-игра Тщеславие (переписана целиком в правке 18).
- `src/minigames/sloth/plant-model.js` — структурная модель растения для мини-игры Лень (правка 24).

## Правка 18 (2026-08-16): Тщеславие — с нуля, по новому геймдизайну пользователя

Старая механика (шахматный мат в 1 ход / карты / кости с мгновенной победой
по одному клику) полностью выброшена — вместе со всей старой разметкой
(доска, фигуры, карты, кости) и старым pride.css/pride.js. Новая идея:
персонаж (через WormRenderer, не хардкод-SVG, как было раньше) стоит на
постаменте в центре квадратного игрового поля с рамкой (тёмно-коричневая
окантовка + белая + тёплый тан-фон, по референс-скриншоту пользователя). В
случайных точках поля появляется цель (кружок для тапа) со сжимающимся за
1.5 сек светящимся кольцом вокруг неё — кольцо и есть таймер на тап, стартует
в 2.6 раза больше радиуса цели (в допуске 2-3x из ТЗ), отступ от края поля
= стартовому радиусу кольца, чтобы оно никогда не вылезало за рамку.

Индикация результата — не текст и не иконка, а свечение внутренней окантовки
самого игрового поля (inset box-shadow с большим блуром, гаснет через
прозрачность к центру): красным — промах мимо цели или не успел тапнуть
(кольцо схлопнулось), белым — удачный тап мимо силуэта персонажа
("вспышка фотоаппарата"), розовым — удачный тап ПО силуэту персонажа
("поцелуй"). "На модельке или нет" определяется по `getBoundingClientRect()`
группы `.worm-root` в момент спавна цели — тот же приём, что и в
gluttony.js (bbox отрисованного силуэта, не захардкоженные координаты).

Локальный эффект на месте тапа: при "поцелуе" — эмодзи 💋 (рост ×4.6, сдвиг
в сторону, поворот ~10°, исчезновение через прозрачность, 0.5 сек), при
"вспышке" — нарисованный инлайновым SVG значок вспышки фотоаппарата (кружок
+ расходящиеся лучи, не эмодзи), рост ×5 и исчезновение за 0.2 сек, без
сдвига/поворота (специально проще и быстрее, чем поцелуй).

Шкала мини-игры — свой отдельный счётчик 0..100 (НЕ то же самое, что
`GameManager.sins.pride.value`, который выставляется в 100 только при
заполнении этой шкалы целиком — тот же паттерн, что и в envy.js/gluttony.js):
удачный тап (любой из двух видов) = +10%, промах/пропуск = -10% (не ниже 0),
плюс постоянный пассивный распад -2%/сек, независимо от тапов. Из-за
распада реально нужно около 11 удачных этапов подряд, а не 10 — так и
задумано пользователем.

### Раскладка персонажа на постаменте
Тот же принцип, что и в `layoutFeedStage()` (см. вывод правки 17 ниже):
никаких захардкоженных координат персонажа. `layoutStage()` в pride.js
меряет реальный `getBoundingClientRect()` группы `.worm-root` ПОСЛЕ первого
тика рендерера (двойной `requestAnimationFrame` после mount/update — сегменты
получают transform только в tick(), не раньше) и переставляет персонажа
через `setPosition()` так, чтобы силуэт был centered по X поля, а нижний
край силуэта лежал на постаменте (небольшой нахлёст `FEET_OVERLAP=8px`,
чтобы визуально "стоял на", а не парил над ним). Переживёт будущее
взросление без правок кода, как и раскладка Чревоугодия.

### index.html
Помимо новой разметки `#pride-game` (поле/постамент/контейнер персонажа/
слои целей и эффектов/полоса прогресса/оверлей победы вместо старой
доски+карт+костей), в кэшированной копии index.html в проекте отсутствовали
теги `<script src="src/core/worm-model.js">` и
`<script src="src/core/worm-renderer.js">` — при том что gluttony.js уже
жёстко на них завязан и, по всем признакам, реально работает у пользователя.
Скорее всего, эти теги уже есть в РЕАЛЬНОМ файле на стороне пользователя
(см. вывод ниже про то, что диффы для index.html применяются только через
присланный файл целиком, а не текстом), а в кэш проекта просто попала более
старая версия файла. На всякий случай эти два тега добавлены явно (идемпотентно
— если они уже есть в реальном файле, отправленный целиком index.html всё
равно будет корректным, просто с одной их копией). **Стоит проверить у
пользователя, что в его реальном index.html эти два тега точно есть и стоят
до `worm.js`/`gluttony.js`/`pride.js`.**

### Проверка
Живого браузера по-прежнему нет — код прогнан только "на бумаге"
(перечитан на предмет согласованности имён id/классов между pride.js
и новой разметкой index.html, порядка слоёв z-index, единственности
источника константы длительности кольца — `PRIDE_CONFIG.RING_SHRINK_MS`
задаётся один раз в JS и применяется к CSS transition через inline-style,
а не дублируется в pride.css). Ждём фидбек по итогу живого теста.

## Правка 17 (2026-08-16): раскладка этапа кормления — по РЕАЛЬНЫМ измеренным габаритам, не по захардкоженным anchorX/anchorY

Скриншот показал: персонаж на этапе кормления частично уходит за нижний
край экрана, а кастрюля никак не связана с его положением (стоит в своём
углу). Запрос: расположить персонажа целиком на экране (по горизонтали —
центр, по вертикали — чуть ниже центра, т.к. поза высокая), оставить место
под кастрюлю сверху, и **кастрюля должна всегда стоять точно над головой**,
независимо от текущей позиции/размера персонажа — включая будущее
взросление (новые сегменты, другой размер тела).

### Почему нельзя было просто подобрать новые anchorX/anchorY на глаз
Раньше `anchorX: 0.16, anchorY: 0.8` — два захардкоженных числа. Это
сработало бы только для ОДНОГО конкретного размера персонажа на ОДНОМ
конкретном размере сцены. Как только появится взросление (силуэт станет
больше/другим), эти числа снова придётся вручную пересчитывать — то есть
проблема не решается, а откладывается. Нужен был механизм, который сам
меряет, сколько места персонажу реально нужно, и сам подстраивается.

### Решение
1. В `worm-renderer.js` (правка 17) у хэндла, который возвращает
   `WormRenderer.mount()`, появились `setPosition(x, y)` и `getPosition()` —
   точечная перестановка "точки стояния" персонажа (той самой пары
   `wormX/wormY`, от которой считается голова и вся цепочка) уже ПОСЛЕ
   монтирования, в тех же координатах, что и `getBoundingClientRect()`
   контейнера. Раньше это можно было задать только один раз при `mount()`
   через `anchorX`/`anchorY` — и никогда не менять потом.
2. В `gluttony.js` `alignPotToMouth()` (двигала только `left` кастрюли по
   рту) заменена на `layoutFeedStage()`:
   - меряет фактический bbox персонажа (`.worm-root`, НЕ `svgRoot` — сам
     `<svg>` растянут на весь контейнер через viewBox и не годится) и
     bbox головы через `getBoundingClientRect()`;
   - вычисляет отступы силуэта от центра головы в каждую сторону
     (up/down/left/right extent) — у персонажа они разные, т.к. хвостовая
     часть тянется в одну сторону намного дальше;
   - выбирает новую точку головы: по X — центр сцены с поправкой на
     несимметричность силуэта, по Y — чуть ниже центра сцены, но зажато
     между "снизу тело влезает целиком" и "сверху есть место под кастрюлю
     (высота кастрюли + зазор)";
   - переставляет персонажа туда через `setPosition()`;
   - ставит кастрюлю (`left`/`top` в px) строго над новым положением
     головы: центр по X совпадает с центром головы, дно — на небольшом
     зазоре над макушкой.
3. Раскладка пересчитывается: один раз при входе на этап кормления (после
   двойного `requestAnimationFrame` — сегменты напольной цепи получают
   свои `transform` только в первом тике рендерера, не в момент
   `mount()`/`update()`, так что мерить нужно ПОСЛЕ него), и повторно на
   `resize` окна, пока этап активен.
4. Это работает для ЛЮБОГО размера/формы силуэта, потому что ничего не
   зашито в константы — числа получаются из фактической геометрии SVG.
   Когда появится взросление, `wormHandle.update(model)` изменит модель,
   а следующий вызов `layoutFeedStage()` (уже стоит на входе в этап)
   пересчитает и позицию персонажа, и позицию кастрюли заново, без правок
   этого кода.
5. `bellyGrowthAnchor: 'bottom'` (был задан ещё раньше) здесь оказался
   кстати: живот при кормлении растёт строго вверх, нижний край
   зафиксирован — значит `downExtent`, посчитанный ОДИН раз в начале (живот
   ещё не раздут), остаётся верным на всё кормление, и раскладку не нужно
   пересчитывать каждый кадр.

### Проверка
- `node --check` — синтаксис обоих файлов ок.
- Полный набор старых headless-тестов (`chaintest`, `smoketest`,
  `phasetest`, `geomtest`, `zordertest`, `breathtest`, `speedtest`) —
  без регрессий, `setPosition`/`getPosition` ничего не задели.
- Новый `gluttonylayouttest.js`: `getBoundingClientRect` для sandbox
  подделан НЕ константой, а реальным вычислением bbox из фактических
  `transform`/`rx`/`ry` атрибутов отрисованного SVG (учитывает
  `rootTransform`, `scale(-1,1)` при `flip`, локальные transform частей) —
  проверяет на трёх разных размерах сцены (320×500, 340×640, 280×380), что:
  персонаж целиком внутри сцены со всех 4 сторон; кастрюля центрирована по
  X на голове; дно кастрюли выше макушки; кастрюля не вылезает сверху сцены.
  Все три сценария — OK.
- Санити-проверка: тест прогнан на копии `gluttony.js` с ОТКАЧЕННЫМ фиксом
  (старые `anchorX:0.16/anchorY:0.8`, вызов `layoutFeedStage()` вырезан) —
  тест корректно упал на всех трёх сценариях с теми же симптомами, что
  жаловался пользователь ("Персонаж вылезает СНИЗУ за сцену!", "Кастрюля НЕ
  по центру головы по X!") — подтверждает, что тест реально ловит баг, а не
  проходит вхолостую.
- По пути найден и исправлен баг в самом тесте (не в игровом коде): мерить
  bbox персонажа нужно по группе `.worm-root`, а не по `svgRoot`
  (внешнему `<svg>`, у которого `getBoundingClientRect()` равен размеру
  контейнера) — этот же нюанс учтён и в самом `layoutFeedStage()`.

Живого браузера по-прежнему нет — ждём фидбек, особенно по тому, как живот
раздувается на новом рендере и как двигаются сегменты тела.

## Правка 16 (2026-08-16): напольная цепь тоже получила "своё" расстояние — но не плоское число, а от радиусов

Скриншот показал: напольная цепь (живот→growing-1→growing-2→хвост) сильно
заходит друг на друга, особенно growing-1 почти целиком под животом.
Пользователь попросил "аналогично как с головой и сегментами тела" —
увеличить базовое расстояние.

### Почему нельзя было просто скопировать число из правки 13
Вертикальная часть (`WORM_VERTICAL_SPACING=26`) — это ОДНО число, потому
что после правки 15 сегмент-1/сегмент-2/живот ВСЕ одного радиуса (23) —
для равных кругов одна и та же дистанция даёт одинаковый нахлёст везде,
плоская константа там уместна. Напольная цепь — ПРОТИВОПОЛОЖНЫЙ случай: её
радиусы сужаются (живот 23 → growing-1 17 → growing-2 15 → хвост ~10.5).
Плоская константа для сужающейся цепи математически не может подойти сразу
всем парам: значение, достаточное чтобы разъединить крупную пару
живот↔growing-1 (сумма радиусов 40), для мелкой пары growing-2↔хвост
(сумма 25.5) уже слишком много — вернуло бы зазор, который специально
чинили в правках 13 и 15 (п.4).

### Решение
Базовая длина звена теперь считается ОТДЕЛЬНО для каждой конкретной пары —
от их РЕАЛЬНЫХ радиусов, не от одной общей константы:
`floorLinkBaseLength(rA, rB) = rA + rB - WORM_FLOOR_LINK_OVERLAP_RATIO *
min(rA, rB)`, `WORM_FLOOR_LINK_OVERLAP_RATIO = 0.6`. Смысл: у любой пары
(крупной или мелкой) сохраняется одно и то же ОТНОСИТЕЛЬНОЕ (не
абсолютное) перекрытие — 60% от радиуса МЕНЬШЕГО из двух остаётся "утоплено"
друг в друге. Крупные пары автоматически получают заметно больше
расстояния, мелкие (хвост) остаются тесными — прямое продолжение подхода
из правки 13 (там радиус основания хвоста уже считался от радиуса соседа,
не был плоским числом).

Результат по факту (radii: живот 23, growing-1 17, growing-2 15, хвост
10.5): живот↔growing-1 = 29.8px (было 18), growing-1↔growing-2 = 23px
(было 18), growing-2↔хвост = 19.2px (было 18, +4 на ходу — 23.2px, сумма
радиусов 25.5px, перекрытие с запасом 2.3px — зазор не открылся заново).
"Доп. растяжение на ходу" (`WORM_MOVE_SPACING_EXTRA=4`) складывается ПОВЕРХ
этой базы одинаково для всех пар — не масштабируется вместе с радиусами,
это отдельный, не завязанный на размер эффект.

### Проверка
- `node --check` — синтаксис ок.
- `chaintest.js` переписан: раньше проверял ОДИН общий диапазон расстояний
  для всей цепи, теперь — свой диапазон для каждой конкретной пары
  (`belly->growing-1`: [29.8, 33.8], `growing-1->growing-2`: [23, 27],
  `growing-2->tail`: [19.2, 23.2]) — все 1800 замеров легли в свой диапазон.
- `geomtest.js` обновлён под ту же формулу для расчёта ожидаемого зазора
  хвоста — перекрытие -2.3px (запас, а не впритык).
- `smoketest.js`, `phasetest.js`, `zordertest.js`, `breathtest.js`,
  `speedtest.js` (обновлён порог под новую базу 19.2 вместо 18, с
  проверкой что тест по-прежнему ловит регрессию) — без регрессий.

## Правка 15 (2026-08-16): интервал у головы, равные сегменты, дыхание живота, скорость вместо бинарного флага
`WORM_CHAIN_HEAD_GAP` 34→29. Сегмент-1/сегмент-2/живот выровнены по
радиусу (23/23/23), `WORM_MODEL_VERSION` 1→2 (чтобы правка проявилась и у
уже сохранённых в localStorage персонажей). Дыхание — единая фаза на живот
(100%, эталон)/сегмент-2 (70%)/сегмент-1 (30%) через `WORM_BREATH_RATIO`.
`WORM_MOVE_SPACING_EXTRA` 8→4. Растяжение хвоста переведено с
`state.moveIntensity` (сглаженный БИНАРНЫЙ флаг "движется/стоит", ~1с
отставание от реальной остановки) на `state.speedIntensity` (реальная
мгновенная скорость, px/сек) — персонаж тормозит по экспоненте, реальная
скорость падает к нулю задолго до пересечения порога `isMoving`.

## Правка 14 (2026-08-16): Z-порядок слоёв тела пересобран под явную схему
Сортировка стала двухуровневой: сначала СТОРОНА/ГРУППА (напольная часть →
вертикальная часть тела → живот), потом расстояние от живота внутри своей
группы. `zordertest.js` подтвердил DOM-порядок.

## Правка 13 (2026-08-16): форма хвоста + добивание "отрыва" + разрежены сегменты у головы
Основание хвоста — доля (`WORM_TAIL_BASE_RATIO=0.7`) от реального радиуса
предхвостового growing-сегмента. Кончик — закруглённый овал
(`WORM_TAIL_TIP_RATIO=0.35`), не точка. Заведена `WORM_VERTICAL_SPACING`
для вертикальной части.

## Правка 12 (2026-08-16): второй, более коварный тайминг-баг — "эффект волчка"
`Math.sin(state.animTime * wiggleSpeed + фаза)` с переменным `wiggleSpeed`
давал скачок аргумента синуса пропорционально ВСЕЙ прошедшей истории.
Решение: отдельные аккумуляторы фазы (`chainWigglePhase`, `tailWagPhase`),
прирастающие ТЕКУЩЕЙ скоростью каждый кадр.

## Правка 11 (2026-08-16): хвост "отрывался" — переход на кинематическую цепь
Каждый сегмент раньше считал СВОЮ позицию независимой формулой — заменено
на кинематическую цепь. Разъединиться по РАССТОЯНИЮ стало геометрически
невозможно.

## Правки 8-10 (2026-08-16)
Новая "стоящая" поза, анимация ходьбы, слизистый след, первая
(недостаточная) попытка тюнинга амплитуд виляния.

## Правки 1-7 (2026-08-16)
Детализация отрисовки, форма/мимика рта, веко, единый livePose-канал.
(См. предыдущие версии этого файла за подробностями.)

## Прошлые сессии (2026-08-15, этап "Чревоугодие")
Главный экран через общую модельку, пересобран хвост, зазор голова↔тело,
Z-порядок тела, мини-игра Чревоугодие мигрирована на WormRenderer целиком.

## Важный вывод на будущее (закрепить в подходе)
Пользователь тестирует через локальный WebDAV-сервер и **применяет только
файлы, присланные вложением** (`SendUserFile`). Текстовые "было/стало" диффы
для `index.html`/CSS НЕ применяются на практике.

У персонажа должен быть ОДИН источник правды по каждому параметру — модель
(`worm-model.js`) для СТРУКТУРНЫХ данных (радиусы, цвета), живой канал
(`livePose`) для НЕПРЕРЫВНЫХ покадровых величин.

**Если структурное изменение модели должно гарантированно проявиться у уже
сохранённых (в localStorage) тестовых персонажей — поднимать
`WORM_MODEL_VERSION`.**

**Вывод правки 8**: без живого браузера — прогонять логику через
headless-смоук-тест (jsdom + управляемая очередь rAF).

**Вывод правки 9**: новый режим/раскладку по умолчанию использовать ВЕЗДЕ,
не решать самостоятельно за пользователя.

**Вывод правки 10 (уточнён правкой 11)**: несколько сегментов, которые
должны выглядеть физически соединёнными, нужно СЧИТАТЬ как цепь.

**Вывод правки 11**: анимационный тайминг должен считаться от РЕАЛЬНОГО
прошедшего времени (`dt`/`dtSec`), а не от количества кадров.

**Вывод правки 12 (важный, легко повторить снова)**: переменная скорость
анимации → ОБЯЗАТЕЛЬНО отдельный аккумулятор фазы (`phase += dtSec *
скорость`), а не `Math.sin(накопленноеВремя * переменнаяСкорость)`.

**Вывод правки 13**: "не отрывается визуально" — это ещё и про то, что
СИЛУЭТЫ (радиусы) соседних звеньев достаточно большие, чтобы перекрываться
на максимальной длине звена.

**Вывод правки 14**: когда порядок слоёв зависит от "стороны"/"группы"
элемента — сортировка должна быть ДВУХУРОВНЕВОЙ.

**Вывод правки 15 (обобщает 12)**: величина, управляющая непрерывной
анимацией, должна отражать РЕАЛЬНУЮ физическую величину (например,
скорость), а не сглаженный БИНАРНЫЙ прокси-флаг с собственным временем
отклика.

**Вывод правки 16 (важный, применим ко ВСЕЙ цепи, не только к хвосту)**:
если цепь СУЖАЕТСЯ (радиусы соседних звеньев разные), НИКАКАЯ одна плоская
константа расстояния не может одновременно быть "разъединяющей" для
крупных пар и "неоткрывающей зазор" для мелких — базовое расстояние между
КАЖДОЙ конкретной парой нужно считать от ИХ РЕАЛЬНЫХ радиусов (сумма минус
доля перекрытия от меньшего радиуса), а не от одного числа на всю цепь.
Плоская константа корректна ТОЛЬКО когда соседние звенья одного радиуса
(как вертикальная часть после правки 15). При правке расстояния — сначала
проверить, одинаковые ли радиусы у сегментов этого участка цепи.

**Вывод правки 17 (важный, общий принцип для ЛЮБОЙ раскладки под конкретную
сцену/мини-игру)**: если позиция персонажа на экране зависит от его
реального размера (а размер в будущем изменится — взросление), нельзя
кодировать позицию парой захардкоженных чисел (`anchorX`/`anchorY`) —
нужно ИЗМЕРЯТЬ фактический отрисованный bbox через `getBoundingClientRect()`
уже ПОСЛЕ первого тика рендерера (сегменты получают `transform` только в
`tick()`, не в `mount()`/`rebuild()`) и позиционировать от него. Для этого
рендерер должен уметь переставлять персонажа уже после монтажа —
отсюда `setPosition()`/`getPosition()` в публичном API. То же самое
относится к любому объекту сцены, который должен быть "привязан" к
конкретной части персонажа (кастрюля к голове, в будущем — что угодно
ещё): его позицию тоже нужно каждый раз пересчитывать от измеренной
позиции этой части, а не от отдельно заданных координат.

## Открытые/отложенные темы (на следующие сессии)
- Миграция мини-игр на общую модельку: **Похоть (Lust) следующая**, затем
  Гнев (Wrath, Canvas в `wrath.js`), затем остальные.
- Механика взросления (`growthStage`) — не решена, но раскладка Чревоугодия
  (правка 17) уже готова её пережить без правок кода.
- Настроение персонажа / поведенческий слой на главном экране — архитектура
  готова, не хватает логики "что триггерит улыбку/грусть/болтовню".
- Точный алгоритм генерации шрамов — не доработан.
- Система слотов аксессуаров — не реализована.
- Лёгкий изгиб вертикальной части тела (по наброску пользователя там был
  небольшой изгиб, сейчас строго прямая линия) — можно добавить по фидбеку.
- `anchorX`/`anchorY` в `worm.js` не трогались — см. правку 9. В
  `gluttony.js` (правка 17) это теперь только СТАРТОВАЯ точка, реальная
  позиция считается в `layoutFeedStage()`.
- Слизистый след/анимация ходьбы завязаны только на `opts.wander`.
- Настоящая физика (столкновения с другими объектами сцены) — отложена до
  появления конкретного кейса.
- `wander` использует непроинициализированный `Math.random()` — тесты
  из-за этого не полностью детерминированы между прогонами; не критично.
- `WORM_FLOOR_LINK_OVERLAP_RATIO=0.6` (правка 16) и `WORM_SPEED_REF_PX_PER_SEC=60`
  (правка 15) подобраны по расчёту, не глазами — возможна ещё одна итерация
  после живого просмотра.
- `MARGIN=10`/`POT_GAP=6` в `layoutFeedStage()` (правка 17) — тоже подобраны
  расчётом, не живым просмотром; возможна тонкая подстройка после фидбека.
- Ждём подтверждения от пользователя по правкам 16 и 17 — живого
  браузерного теста после них ещё не было.
- **Лень (правка 24)** — структурная переработка растения и полки с
  инструментами ещё не проверена вживую пользователем; ждём фидбек.
  Числовые константы (rootLen=22%, lateralMax=40%, положение полки)
  подобраны расчётом/headless-тестом, возможна доп. подстройка.

## Файлы (актуальные версии сохранены в проекте)
- `claude/worm-model-architecture.md` — не менялся.
- `claude/worm-model.js` — правка 15: сегмент-1/сегмент-2/живот выровнены
  по радиусу (23/23/23), `WORM_MODEL_VERSION` 1→2. В правках 16-24 не менялся.
- `claude/worm-renderer.js` — правка 11: кинематическая цепь; правка 12:
  фаза покачивания через отдельные аккумуляторы; правка 13: форма/размер
  хвоста, вертикальный интервал у головы; правка 14: двухуровневый
  Z-порядок; правка 15: интервал у головы 34→29, градиент дыхания, доп.
  растяжение на ходу 8→4, растяжение от реальной скорости; правка 16:
  базовая длина звена напольной цепи — от реальных радиусов каждой пары
  (`floorLinkBaseLength`/`WORM_FLOOR_LINK_OVERLAP_RATIO`), не плоское число;
  правка 17: публичные `setPosition(x,y)`/`getPosition()` у хэндла монтажа.
- `worm.js` — не менялся; сюда в будущем ляжет логика мимики.
- `gluttony.js` — правка 9: `pose` не передаётся (стоящая поза). Правка 17:
  `alignPotToMouth()` заменена на `layoutFeedStage()` — раскладка персонажа
  и кастрюли по реально измеренным габаритам, а не захардкоженным
  `anchorX`/`anchorY`; пересчитывается на входе в этап и на `resize`. В
  правках 10-24 не менялся.
- `plant-model.js` — новый (правка 24): структурная модель растения Лени.
- `sloth.js` — правка 24: см. подробности выше (кубическая кривая стебля,
  `stemPointAt()`, `drawRootFlare()`).
- `sloth.css` — правка 24: `.pot-wrap` 42%→78%, `.tools-row`→`.tools-shelf`.
