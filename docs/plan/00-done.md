# Где проект сейчас

Единственное место, где написано состояние дел. Всё остальное в `docs/plan/` —
это ЗАМЫСЕЛ каждого куска, а не отчёт о нём. Обновлять здесь, а не заводить
второй такой файл.

---

## Ядро — сделано и переписыванию не подлежит

| Что | Где | Состояние |
|---|---|---|
| Ленивое время: шкалы, оффлайн, кулдауны — формулой от метки | `core/time.js` | Работает, инвариант 1 |
| Состояние со схемой и миграциями | `core/state.js` | Работает, инвариант 5 |
| Переходник к «правде» — единственное место начислений | `core/backend.js` | Работает, инвариант 2 |
| Все числа баланса в конфиге | `config/economy.js` | Работает, инвариант 3 |
| Холст 390×844, масштаб целиком | `core/stage.js` | Работает, инвариант 11 |
| Общее окно мини-игры | `core/minigame-window.js` | Написано; **на нём шесть игр из семи**, зависть — долг |
| Меню грехов: колесо лучей | `core/sins-menu.js` + `sins-menu.md` | Работает: удержание на червя открывает, узел горит по порогу голода, тап запускает игру. Шкал в комнате больше нет, инвариант 10 |
| Струя жидкости: один движок на все наливы | `core/liquid-stream.js` | Работает: кастрюля и кормёжка на кухне, лейка в саду |
| Персонаж: структурная модель, рендерер, отметины | `core/worm-*.js` | Работает; взросление не решено |
| Пищеварение и кучка на полу | `core/backend.js` + `06-digestion.md` | Работает |
| Довольность, истощение, смерть | `core/worm-condition.js` + `07-condition.md` | Работает |
| Комнаты данными | `core/rooms.js` + `08-locations.md` | Механика есть, комната пока одна |
| Debug-режим и машина времени | `core/debug.js` | Работает |

Игра открывается без Telegram (инвариант 7), сервера нет и по плану пока не
нужно — форма API уже задана переходником.

---

## Грехи

| Грех | Замысел | В коде | Чего не хватает |
|---|---|---|---|
| **Гнев** | `03`, `09`, `10`, `13` | Лобби со слотами, бой на общей модели, магазин, прокачка, рогалик-забег, пассивки. Без слов. На общем окне | Мясо с боссов не выдаётся в кухню |
| **Чревоугодие** | `20` | Кухня целиком: холодильник → доска → нож → плита → кормёжка. Своя графика, диегетический интерфейс, общий движок струи, debug-панель, два прогонных теста. На общем окне | Магазин-смартфон, апгрейды, бусты жидкостей |
| **Лень** | `19` | Сад с грядками: участок с панорамой, шесть грядок, круг «разобрал завал рукой → лунка лопатой → семя → лейка → грабли → рука», два этапа по времени, каждое действие делается руками (дёргать лопату, приминать землю, водить граблями), полив удержанием на общей струе, заваленные грядки за жетоны лени, семена конечные в мешке с ячейками, бесконечная трава как пищеблок сада, сено со всех растений, находки в земле по градации редкости, растение семействами из сида, время цифрами в кольце, шкала от пребывания и действий. Без слов, на общем окне | Магазин: семена за сено, новые виды и прокачки за жетоны. Лестницы лейки, лопаты, граблей и возврата семян посчитаны — купить их пока нечем |
| **Зависть** | `18`, `envy-cloud` | Облако наклеек, пул образов, векторные образы | Добыча, риск, несгораемые деления, прокачка из плана НЕ сделаны |
| **Тщеславие** | `17` | Костюмерная со слотами наряда и витриной, смена декораций с отсчётом, восемь предметов гардероба (носятся во всех грехах). Ковровая дорожка: червь идёт на зрителя, машина по центру подвозит и уезжает назад шкалой выхода, перспектива с толпой и ограждениями, шаг на месте и хвост в глубину, покерфейс, вспышки и поцелуи, СТРИК вместо ажиотажа (множитель растят только белые зоны, поцелуйные не появляются при нулевом стрике, любая незакрытая зона рвёт серию), три линии покупок (толпа — ширина зоны, дорожка — её время, машина — множитель; частота зон скрытая, от суммы ступеней), ПОРОГ ГОЛОДА вместо убывающей доходности (платят, только пока шкала греха ≤ 50; сытому червю не спавнят поцелуйных зон вовсе, но заработанное не режется никогда). Лестницы посчитаны на два оплаченных выхода в сутки — 199 дней, и фарм их не ускоряет. Без слов, на общем окне | Остановка для позы, новые типы зон, второй ряд гардероба |
| **Алчность** | `16` | Автомат за монеты: крутка стоит монету и наливает шкалу на 10% (полная = 10 круток), символы с ВЕСАМИ и таблица выплат в конфиге, розыгрыш в `Backend.greedSpin()` — мини-игра не решает ничего. Возврат 75.5% при 27% выигрышных круток, точный перебор в `tools/sim-slots.js`. Гарантия против тильта подменяет исход (шестая пустая крутка встаёт на тройку костей), выплата НЕ режется порогом голода — это возврат ставки, а не награда. На экране без слов: цена у рычага, кошелёк в лотке, таблица выплат на корпусе, шкала — колба в пилястре. На общем окне | Прокачки нет и просто так не завести: любая ступень двигает возврат, а он посчитан под коридор |
| **Похоть** | `21` | Старая мини-игра | Ванная, четыре этапа, финал на меткость — НЕ сделаны |

Читается так: **гнев, чревоугодие, лень, похоть и тщеславие доведены до
играбельного вида, на старой механике осталась одна зависть — под неё
замысел уже написан.**

---

## Экономика

Посчитано и лежит в конфиге: мера силы и кривая победы (`15`), бюджет золота и
сток через алчность (`14`, `16`: возврат 75.5%, шкала стоит 2.4 монеты), доход дорожки тщеславия (`17`), добыча
зависти (`18`), пропускная способность сада (`19`), меткость похоти (`21`),
качество блюда по типам (`20`).

Валюты в `ECONOMY.currencies`: золото, кристалл, жетоны и осколки гнева и
кухни, какашки. Размен «три осколка = жетон» складывается сам.

Числа проверяются калькуляторами в `tools/` по ЖИВОМУ конфигу — не по копии.
Правило: **сначала посчитать, потом добавлять контент.**

---

## Долги, которые видно уже сейчас

1. **Одна мини-игра не на общем окне** (инвариант 8): зависть. Рисует себе
   рамку и крестик сама.
2. **Взросление персонажа** (`growthStage`) не решено. Раскладки, считающие
   позицию по измеренным габаритам, его переживут; захардкоженные — нет.
3. **Источники ингредиентов кухни подключены наполовину**: сад кладёт овощи и
   специи в ту же кладовую, из которой берёт кухня, и потолок склада работает.
   Мясо с боссов гнева и жидкости из магазина — ещё нет.
4. **Магазины и апгрейды есть только у гнева и тщеславия.** У тщеславия он
   без экрана вовсе: ценник висит на самом предмете, который покупка меняет
   (машина, толпа, ковёр). У кухни, сада, зависти и алчности прокачки
   посчитаны, но покупать их пока нечем. У алчности прокачки нет вовсе, и
   это не забывчивость: любая ступень автомата двигает возврат, а он
   посчитан под коридор 60–80% — продавать там можно только то, что возврат
   НЕ трогает.
5. **Уведомлений нет** (`12`) — и не будет до сервера, там же и написано
   почему.
6. **Круг «удобрение → урожай → блюдо → какашки» печатает ресурсы** на верхней
   ступени лейки (+6 💩 в сутки). Решение отложено до живых тестов сознательно,
   три способа починки замерены — `19-sloth-garden.md`, раздел 3. Проверка
   встроена в `tools/sim-garden.js` и ругается сама.

---

## Что делать дальше

Порядок не жёсткий, но осмысленный:

1. Довести до конца остальные грехи по уже написанным замыслам — осталась
   зависть (`18`).
2. Переселить на общее окно последнюю мини-игру — зависть.
3. Связать грехи между собой: сад → кухня, боссы → мясо, зависть → жидкости.
   Именно эти связи и делают из семи мини-игр одну игру.
4. Магазины и апгрейды — после того, как механики проверены вживую.

Спор с исходным планом и почему порядок именно такой — `05-next-steps.md`.
Это архив: решения оттуда уже приняты и лежат в инвариантах.
