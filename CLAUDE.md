# Свиночервь Тамагочи — рабочие правила

Мобильная HTML5-игра: тамагочи, где потребности персонажа — семь смертных
грехов, у каждого своя мини-игра. Чистая статика, сборки нет.

Этот файл — всё, что нужно знать до первой правки. Остальное подтягивается по
маршрутизатору ниже, а не читается заранее.

---

## 1. Инварианты (нарушение = переписывать потом)

1. **Никаких тикающих таймеров состояния.** Значение шкалы, оффлайн-доход,
   кулдаун — всё считается формулой от `updated_at` и текущего времени, а не
   накручивается в `setInterval`. Таймеры на клиенте — только для анимации.
2. **Мини-игры ничего не начисляют.** Они сообщают результат
   (`sin`, `mode`, `outcome`, `meta`), а что за это дать — решает конфиг
   наград. Прямые записи вида `GameManager.sins.X.value = 100` запрещены.
3. **Числа баланса живут в конфиге**, не в коде мини-игр. Конфиг потом
   переезжает на сервер как есть.
4. **Всё время — через `time.js`** и серверную поправку. Локальные часы
   игрока не источник правды.
5. **Состояние сохраняется с номером схемы и мигрируется.** Не сбрасывается
   при смене формата: на сейве висит прогресс, который копился неделями.
6. **Координаты всего, что живёт на теле** (шрамы, косметика, следы грехов) —
   нормализованные, привязанные к скелету (`zone` + `t`), не в пикселях.
   Иначе всё поедет при взрослении и эволюции.
7. **Игра обязана открываться без Telegram.** Нет `initData` → локальный
   игрок. Это штатный режим, на нём идёт вся разработка и тестирование.
8. **Окно мини-игры одно на все грехи.** Мини-игра не рисует себе рамку,
   крестик и вопрос при выходе — их надевает `src/core/minigame-window.js`.
   Все семь открываются на весь экран: рамка идёт по краю холста без
   отступа, крестик в одном и том же углу, вопрос «выйти?» один на всех.
   Комната под открытой мини-игрой не просто останавливается, а перестаёт
   рисоваться (`#game-container.mg-open`). Своя карточка, свой крестик,
   выход без вопроса — так больше нельзя (`docs/minigame-window.md`).
9. **Ни одного слова в игре.** Интерфейс говорит иконками, цветом,
   движением и числами; буквы в игровом холсте запрещены. Это снимает
   локализацию и держит механики честными: то, что нельзя показать, надо
   переделать, а не подписать. Цифры разрешены — цифра не слово, а
   пиктограмма количества. Не игра и под правило не попадает: debug-панели,
   заголовок вкладки и манифест. Словарь замен — `docs/plan/11-no-words.md`.
   **Исключение ровно одно, и оно объявлено:** логотип приложения доставки в
   телефоне кухни. Логотип — картинка, а не подпись: он ничего не объясняет
   и не переводится. Записан поимённо в `tools/test-no-words.js`, чтобы
   проверка осталась настоящей, — исключение, снимающее проверку с целого
   экрана, это отменённая проверка. Новых сюда не добавлять.
10. **Шкал грехов в комнате нет.** Все семь живут в одном экране — колесе
    лучей, которое открывается удержанием пальца на самом червя и
    закрывается таким же удержанием на логотипе в центре
    (`src/core/sins-menu.js`, `docs/sins-menu.md`). Верхних панелей, списков
    со шкалами и кнопок «утолить» в комнате не заводить: место на экране —
    для червя и его жилья. Узел горит тогда, когда за заход заплатят, но
    ЧЕМ это меряется — дело каждого греха: у лени платят за выращенное, у
    алчности захода без конца нет вовсе. Чтение греха подменяемое
    (`SinsMenu.readers`): у алчности своя читалка (к порогу добавлена монета
    в кошельке — без неё крутка не состоится), у гнева своя (порога голода у
    боя нет вовсе, мерой служит здоровье бойца), остальные пять живут на
    умолчании — шкала и порог голода `ECONOMY.sins.<грех>.payAt`. Это
    умолчание, а не закон: общего правила «горит по половине шкалы» в колесе
    нет и заводить его не надо.
11. **Вся игра живёт в холсте постоянного размера** 390×844 условных единицы
    (`src/core/stage.js`), который целиком масштабируется под окно. Верстать
    можно в этих единицах: раскладка не зависит ни от поворота телефона, ни
    от формы окна. Резиновая вёрстка здесь не нужна и вредна —
    `docs/stage-and-room.md`.
    **Безопасная зона вычитается РОВНО ОДИН раз и снаружи холста**
    (`Stage.viewport`). Внутри холста `env(safe-area-inset-*)` быть не должно
    ни одного: он уже стоит в безопасной области целиком, и второй отступ
    отъедает место повторно — да ещё в пикселях устройства, а холст
    масштабирован. На компьютере это невидимо (зоны там нулевые), на телефоне
    срезает раскладку. Ловушка №134, стережёт `tools/test-stage-fit.js`.
    Отсюда же: срез сверху и снизу у svg мини-игр (`slice`) намеренный, и
    всё, что обязано быть видно, живёт внутри него С ЗАПАСОМ, а не у кромки
    холста.

---

## 2. Маршрутизатор: что открыть под задачу

### Сначала — верстак

**Верстак — рабочее место под одну задачу: станок, чертёж, шаблон, проверка
и грабли именно этой темы в ОДНОМ файле.** Если задача попадает в строку
ниже, открывать надо только её: верстак сам скажет, куда смотреть дальше.

| Задача | Верстак |
|---|---|
| **Что-то на теле ведёт себя странно при повороте** | **`docs/bench/turn.md`** — три способа ответить на ракурс и словарь симптомов |
| Новая вещь в гардероб; как одежда сидит и ведёт себя на развороте | **`docs/bench/wear.md`** |
| Висящее качается не туда: наклон телефона, парные копии, точка крепления | **`docs/bench/wear.md`**, разд. 7а + `tools/test-tilt.js` |
| Одежда не поспевает за размером тела: раздутие живота, дыхание | **`docs/bench/wear.md`**, разд. 7б |
| Одежда одной части лежит поверх другой, которая впереди | **`docs/bench/wear.md`**, разд. 7б («про глубину») |
| Новый вид отметины на коже: шрам, ожог, шов | **`docs/bench/marks.md`** |
| **Правлю внешность персонажа руками: что нажимать и что означает ручка** | **`docs/bench/look.md`** — справочник по инспектору, пульту и студии: три режима, шесть накладок, что тянется пальцем, а что нет, и что нельзя |
| **Хочу поменять ФОРМУ части: «ухо поострее, мочка ниже», «горб на загривке»** | **`docs/bench/look.md`, разд. 12** — контур тянут за именованные точки: двенадцать у черепа, пять у уха, обхват у каждого звена тела. Экран — 🐞 → «студия» (`src/core/worm-studio.js`) |
| Тело персонажа: части, слои под кожей, поза, ходьба, кадры | **`docs/bench/worm.md`** |
| Комната, пол, холст, камера; новая локация | **`docs/bench/room.md`** |
| Новая мини-игра или новый экран внутри греха | **`docs/bench/minigame.md`** |
| Цены, награды, скорости, прокачка — всё, что считается | **`docs/bench/numbers.md`** |
| **Сколько ступеней в ветке и почём** | **`docs/plan/15-progression.md`, разд. 4а** — полгода-год на всё, покупка раз в неделю; растягивать КОЛИЧЕСТВОМ ступеней, а не ценой. Проверяет `tools/sim-progression.js` |
| Новый предмет или фон: цвет, линия, объём, перспектива | **`docs/bench/art.md`** |
| Палец: зоны захвата, жесты, удержание, отдача | **`docs/bench/input.md`** |

Список верстаков и зачем они — `docs/bench/README.md`.

### Чего в верстаках нет

**Замысла** («почему грех устроен именно так») и частностей конкретных
экранов. Это здесь:

| Задача | Куда смотреть |
|---|---|
| **Грабли: наступал ли я на это раньше** | `docs/traps.md` — **не читать подряд**: в начале ОГЛАВЛЕНИЕ по симптомам, открыть только похожие пункты. Верстаки перечисляют номера по своей теме |
| Где проект сейчас, что дальше | `docs/plan/00-done.md` |
| Замысел конкретного греха | `docs/plan/` — по названию (см. `docs/plan/README.md`) |
| Гнев: темп боёв, регенерация, почему нет порога голода | `docs/plan/09-wrath-rework.md`, разд. 6a |
| Гнев: откуда берётся соперник | `docs/plan/09-wrath-rework.md`, разд. 6c |
| Гнев: шапка и подвал лобби, фоны режимов | `docs/plan/09-wrath-rework.md`, разд. 4 |
| Гнев: что работает в бою, а что в забеге | `modes` у веток `ECONOMY…wrath.upgrades`; снаряжение и числовая прокачка в забег не едут |
| Гнев: где что покупается | `src/minigames/wrath/wrath-shop.js` — ВСЁ покупаемое одним рядом полок за 🏪, отдельного экрана прокачки нет |
| Забег: сложности, окно входа, развилка | `docs/plan/10-wrath-rogue.md`, разд. 3a и 8a |
| Переработка шрамов в жетон | удержание пальца на черве в лобби (`WrathLobby.startHold`): шрамы набирают свет КРАСКОЙ через WAAPI — фильтр на внутренней группе svg не рисуется (traps, п. 101) |
| Автомат алчности: веса, выплаты, возврат | `docs/plan/16-greed-slots.md` |
| Ванная и похоть | `docs/plan/21-lust-bath.md` |
| Ковровая дорожка и костюмерная тщеславия | `docs/plan/17-pride.md` |
| Кухня: предметы по референсу, как проверять | `docs/kitchen-objects.md`, `docs/kitchen-checks.md` |
| Лень: переход между грядками, глубина, зум | `docs/plan/19-sloth-garden.md`, разд. 1а, 1б и 1в — панорамы нет, в центре ВСЕГДА одна грядка, ход стрелками до последней открытой плюс одна закрытая, переезд считается покадрово (`glideCam`), а НЕ css-переходом. Четыре слоя глубины (`GARDEN_ART.LAYERS`), у переднего доля хода РОВНО единица — в его координатах считается палец. Зум (`GARDEN_ART.ZOOM`) один на все слои и подобран так, чтобы соседней грядки в кадре не было; перевод «экран ↔ сцена» — только через `toStagePoint`/`fromStagePoint` |
| Лень: магазин | `src/minigames/sloth/sloth-shop.js` — отдельный экран за корзиной на полке: ступени инструментов за жетоны, семена за сено, виды за жетоны. Грядка покупается НЕ там, а на самом завале. Цена лежит рядом со ступенью (`GARDEN.CAN_TIERS`, `RAKE_TIERS`, `SEED_TIERS`, `work.dig.price`), замысел — `docs/plan/19-sloth-garden.md`, разд. 6а |
| Кухня: магазин, цены, апгрейды утвари | `src/minigames/gluttony/kitchen-shop.js` — телефон в углу общего вида; продукты в `KITCHEN.shop.food`, утварь на общем станке прокачки `ECONOMY.minigames.gluttony.upgrades` |
| Меню грехов, ворота награды | `docs/sins-menu.md` |
| Жетон и осколок: как нарисованы и как делятся | `docs/token-art.md` |
| Шрамы и наряд не видны на экране | их кладёт `loadWormModel()` (`withCosmetics`), а не отдельный экран; обновление — `refreshWormMarks()` через шину (traps, п. 98) |
| **Как эта штука на червя называется по-человечески** | **`src/core/worm-parts.js`** — таблица визуальных сущностей: «левая скула», «пятачок», «покров кожи». Три сорта, и от сорта зависит, можно ли вещь ТАЩИТЬ: `node` (узел дерева), `landmark` (точка на кривой черепа — в дереве её НЕТ), `layer` (процедурная масса, крутится только целиком). `identify()` проходит технические обёртки насквозь: тык в голову иначе даёт `head-tilt`, а такой части тела не существует |
| **Смотрю на червя и не понимаю, что не так** | **`src/core/worm-inspect.js`** — инспектор внутри debug-режима (🐞 → «Инспектор»). Два режима выбора: «проблема» (тык → весь контекст для агента одним JSON) и «элемент» (кисть с перебором слоёв). Шесть накладок: свет (блики цепочки в одну линию или гирлянда), силуэт, ракурс (каждая вещь покрашена по своему способу ответа), глубина, плотность, цена. ТОЛЬКО ЧИТАЕТ — правки внешности будет вносить пульт |
| **Почему рендер персонажа устроен именно так; что в нём лишнее** | `docs/worm-render-audit.md` — разбор конвейера «состояние → SVG» замерами, что настоящая необходимость, а что сложность ради сложности. Замысел визуального слоя — `docs/worm-visual-control.md` (три сорта сущностей, почему имена таблицей снаружи, почему обратный перевод меряется) |
| **Хочу поменять внешность: размер, объём, свет, поверхность** | **`src/core/worm-look.js`** — пульт: 37 ручек человеческими именами, значения ОТНОСИТЕЛЬНЫЕ (`headSize: +0.15` = «на 15% больше»), ноль = «как задумано». Заморозка настоящая: `lock: ['eyes']` отказывает с причиной. Откат и сброс, накопленный патч наружу. Перетаскивание считает обратный перевод ЗАМЕРОМ чувствительности, а не формулой. Свет (`LIGHT`) живёт не в модели и откатывается отдельно |
| Показать трату или начисление ресурса | `src/core/wallet-fx.js` — цифра вылетает из счётчика сама; отдельных сообщений на экранах не заводить |
| Убрать слова из экрана | `docs/plan/11-no-words.md` |
| Деплой, iPhone, service worker, запуск в Telegram | `docs/deploy-iphone.md` |
| «Почему это сделано так» | `docs/progress-log.md` — архив, читать подряд незачем |

---

## 3. Как проверять

**Правка, которую не открыли в браузере, не проверена.** Тесты ловят «дошло
ли до конца», а не «выглядит ли это игрой».

```bash
python3 -m http.server 8777            # из корня, один раз на сессию
NODE_PATH=/opt/node22/lib/node_modules node tools/<тест>.js /tmp/shot-
```

| Инструмент | Что проверяет |
|---|---|
| `test-kitchen.js` | Кухня целиком: холодильник → доска → нож → плита → кормёжка |
| `test-kitchen-block.js` | Второй путь кухни: пищеблок, страховка от тупика |
| `test-kitchen-shop.js` | Магазин кухни: телефон открывает магазин а не холодильник и не ловит пальцы в чужой фазе, покупка списывает и выдаёт, купленная утварь двигает числа готовки, оба отказа (пустой кошелёк и полная кладовая) ВИДНЫ и ничего не списывают |
| `test-garden.js` | Сад и его стык с кухней: цикл грядки, удобрение, потолок склада, замкнутый круг |
| `test-garden-nav.js` | **Переход между грядками, зум и глубина: грядка встаёт РОВНО в середину экрана (и крайние тоже), левее первой и правее последней доступной хода нет, доступны открытые плюс ОДНА закрытая, стрелки в никуда не существует, свободной панорамы больше нет, СОСЕДНЕЙ ГРЯДКИ В КАДРЕ НЕТ (а своя влезает целиком), зум одинаков у всех слоёв, четыре слоя едут с разной скоростью и передний — ровно на ход камеры** |
| `test-garden-shop.js` | Магазин лени: корзина открывает экран а НЕ берётся в руку, купленная ступень двигает число игры, вид открывается за жетон с первой семечкой, семечка докупается за сено, купленное видно В САДУ, оба отказа видны на пустом кошельке, цена грядки растёт |
| `test-lust.js` | Ванная целиком: кран → мыло → мочалка → хвост → пузыри → финал → награда |
| `test-wrath.js` | Гнев целиком — самый большой прогон в проекте: экономика боя и регенерация, подбор соперника, три режима и их экраны, забег с развилками, лавка, переработка шрамов в жетон, жесты и отдача в палец. **Что именно он проверяет — он же и печатает, по строке на проверку. Список сюда не переносить: он устареет раньше, чем его прочтут.** |
| `test-worm-parts.js` | **Таблица визуальных сущностей: каждая находится и указывает В персонажа, ориентиры черепа лежат НА контуре (спрашивается сам контур, а не та же формула), левое у всех означает одно и то же, ориентиры едут за ракурсом, тык НИКОГДА не возвращает техническую обёртку, кисть схлопывает стопку** |
| `test-worm-inspect.js` | **Инспектор: закрытый НЕ ловит пальцы, панель НЕ накрывает персонажа, кисть перебирает слои по кругу, контекст собирается полным, ползунок ракурса реально поворачивает голову, накладки рисуют, «силуэт» СНИМАЕТСЯ при закрытии, и после всего узлов столько же** |
| `test-worm-studio.js` | **Студия: закрытая НЕ ловит пальцы, экран стоит ПО ХОЛСТУ (и крестик не под шапкой Telegram), персонаж СВОЙ и неподвижный (габарит по кадрам, а не флаг опции), «идёт» возвращает дыхание, камера НАЕЗЖАЕТ на выбранное, палец попадает при наехавшей камере, форму черепа и уха тянут ЗА КОНТУР и она доезжает до пальца, обхват звена тянется отдельно от соседей и не рвёт цепочку, откат снимает ЖЕСТ целиком, вторая сторона не тронута, при закрытии правка уезжает в игру, на КАЖДОЙ из 34 плиток панель влезает без прокрутки и не разрастается больше 260 единиц из 844** |
| `test-head-look.js` | **Голова при ПОВОРОТЕ — пять бед, невидимых на анфасе: предел ракурса ОДИН на игру и редактор (и ползунок студии его спрашивает), на контуре черепа НЕТ излома ни при одном ракурсе, за силуэт не красит ничего кроме ушей, габарит головы честный (клип прячет от глаза, но не от bbox — ловушка 144), ближнее ухо на полном развороте выходит ПЕРЕД головой (и проверка спрашивает КАРТИНКУ — с какой стороны видно больше головы, — а не повторяет за кодом «при плюсе правое»: ловушка 147), бровь остаётся на голове, каждая ручка бровей двигает бровь** |
| `test-worm-look.js` | **Пульт: каждое имя ручки из таблицы сущностей ЕСТЬ в пульте и у каждой части тела есть чем её тронуть, НИ ОДНА ручка не разваливает персонажа (перебираются все подряд в обе стороны — ловушка 143), ручка двигает ГЕОМЕТРИЮ (а не только число), значения относительные и накапливаются, заморозка отказывает с причиной и глаз не трогает, откат снимает ровно последний шаг, перетаскивание доводит вещь ДО ПАЛЬЦА, левая скула не тащит правую, чужой оверрайд не затёрт, выдуманные ручки отвергнуты** |
| `test-scars.js` | Шрамы на коже: внутри РЕАЛЬНОГО контура, без нахлёстов и стопок, столько же, сколько в сейве, три вида различимы, едут с лицом при повороте, не лезут на глаз, пятачок и рот, и не мигают. |
| `test-wardrobe.js` | Как сидит наряд: крой (у вещи есть выкройка, объявлен охват, ткань доходит до краёв части), посадка при пяти ракурсах, надетое на голову едет с лицом, живот не затянут, тело поворачивается а не крутится, висящее висит вниз по экрану и слушается наклона телефона, **вещь растёт вместе с раздутой частью, не рвётся по шву и не лежит поверх того, что стоит впереди**. |
| `test-tilt.js` | **Как висящее слушается гироскопа. Перебирает ВСЕ узлы `[data-swing]`, какие есть на червя, и с каждого спрашивает одно и то же: точка крепления стоит на месте при любом угле, она лежит НА подвеске, свободный конец ниже крепления по экрану, завал вправо уводит конец вправо — у каждой копии, включая зеркальную**
| `test-greed.js` | Автомат алчности: ставка списывается, шкала растёт от каждой крутки, на линии стоит ответ переходника, гарантия против тильта, автомат убыточен, пустой кошелёк не крутит |
| `check-kitchen-fit.js` | **Подгонка кухни под геймплей: опора, зазор, захват, видимость. Секунда, без браузера — гонять после каждой правки координат** |
| `check-perspective.js` | **Перспектива: сходятся ли грани, одинаково ли уходят вглубь соседние плоскости, один ли горизонт на кадр. Без браузера** |
| `audit-food.js` + `audit-food.py` | **Продукты: объём, чернильный контур, силуэт на трёх фонах, подошва, различимость по цвету и форме** |
| `audit-kitchen.js` + `audit-kitchen.py` | **Разбор предметов: развалился ли, есть ли трещины, дыры в сцене, силуэт на фоне, послойная сборка** |
| `audit-bath.js` | **Запечённые предметы ванной по одному, крупно: замкнутость, объём, силуэт, край, законченность (стандарт — в `docs/bake-3d.md`)** |
| `zoom-kitchen.js` | Лупа: кусок кухни крупно, в заданной фазе |
| `shot-kitchen-objects.js` + `diff-kitchen-objects.py` | Склад предметов кухни: снимки и промах относительно референса |
| `audit-frames.js` | **Техосмотр кадра по ВСЕМ экранам: кадры, кадры деформации персонажа, обращения к дереву, чтения раскладки, разбор трассировки. Под замедлением процессора — на быстрой машине разницы не видно** |
| `test-digestion.js` | Еда в кишке: слой вне маски тракта, комок едет покадрово, не вылезает за тело, без еды его не видно |
| `test-ribs.js` | Грудная клетка: рёбра только в животе, анфас половины расходятся по бокам, зеркальный профиль отражается, ближняя половина поверх органов, разворот дёшев |
| `test-slime.js` | След слизи: две группы наложения на весь экран, путь отрезка короткий, объём записей за кадр мал, **на стыках и после остановки нет разрывов, ни одна капля не лежит отдельно от плёнки, кромка и блеск стираются там же, где плёнка**, высохший след исчезает |
| `test-no-words.js` | Ни одного слова на игровых экранах (инвариант 9) — **и ни одного тега, попавшего на экран текстом** |
| `test-sins-menu.js` | Меню грехов: удержание открывает, узел запускает игру, награду даёт только просевший грех |
| `test-lazy-three.js` | three.js не грузится, пока не открыли зависть — и грузится, когда открыли |
| `test-telegram.js` | Мини-приложение: библиотеку Telegram не грузят вне Telegram, внутри — окно развёрнуто, свайп-закрытие запрещено, отдача идёт тактильным мостом |
| `test-stage-fit.js` | **Всё ли помещается в холст: ни одного `env(safe-area-inset)` внутри холста (иначе безопасная зона вычитается ДВАЖДЫ и на телефоне раскладку срезает, а на компьютере это невидимо) и полка сада внутри срезанной области svg с запасом. Ловушка №134** |
| `test-scaled-canvas.js` | **Холст с масштабом НЕ единица и чужим CTM (поведение айфона): палец попадает туда, куда целились, во всех четырёх сценах** |
| `test-pride.js` | Ковровая дорожка: длина выхода, стрик и запрет поцелуев без него, начисление поцелуев, три покупки |
| `sim-progression.js` | **Календарь прокачки ПО ВСЕЙ ИГРЕ: за сколько выкупается всё (цель 180–365 дней) и как часто игрок что-то покупает (цель — раз в неделю). Печатает три профиля присутствия, проверяет среднего. Без браузера** |
| `sim-kitchen-shop.js` | **Числа магазина кухни: по карману ли полка, не собирается ли на ней полное блюдо (мясо и овощи не продаются), и держит ли темп ГОЛОД, а не цена. Без браузера** |
| `sim-rogue.js` | **Забег: доходимость по сложностям, пути развилки и их сочетания. Проверяет, что ни один путь не обязателен. Без браузера** |
| `sim-wrath-year.js` | **На сколько хватает гнева: за сколько дней выкупается всё содержимое, не схлопывается ли бой к концу лестницы, растёт ли доход вдоль пути. Без браузера** |
| `sim-wrath-foe.js` | **Подбор соперника: ровный ли он по силе, разные ли соперники и — главное — двигает ли снаряжение шанс победы вообще. Без браузера** |
| `sim-wrath-regen.js` | **Темп гнева: сколько здоровья съедает бой, через сколько минут следующий, ровен ли цикл при разных сборках, что выходит за час и за сутки. Без браузера** |
| `progression.js`, `sim-*.js`, `measure-power.js` | Числа баланса по живому конфигу |

Прогонные тесты водят пальцем по координатам, а не жмут селекторы: это
заодно проверка, что перевод «экран → сцена» не врёт.

**Гонять их надо и «по-айфонски».** По умолчанию окно прогона — ровно холст
390×844, то есть масштаб РОВНО ЕДИНИЦА, и всё, что перепутало пиксели экрана
с единицами сцены, проходит по совпадению. Две переменные окружения
(`tools/harness.js`) снимают это совпадение:

```bash
SVINO_VIEWPORT=390x700 SVINO_FAKE_CTM=1 \
  NODE_PATH=/opt/node22/lib/node_modules node tools/test-lust.js
```

`SVINO_VIEWPORT` меняет размер окна (масштаб перестаёт быть единицей),
`SVINO_FAKE_CTM` заставляет `getScreenCTM` возвращать матрицу без
трансформации предка — так ведёт себя WebKit на айфоне, а прогоны идут в
Chromium. Прогон, который водит пальцем, обязан считать экранные координаты
через `SvgSpace.toClient`, а не через `getScreenCTM`: иначе он промахнётся
сам и обвинит игру.

**Правило кадров: сначала снять профиль, потом чинить.** Профиль по всем
экранам снимает `tools/audit-frames.js` — он же показывает, чем именно
экран занят: обращениями к дереву, чтениями раскладки или отрисовкой. Есть три признака,
по которым виновник называется без профайлера. Первый: **то, что едет на
видеокарте, идёт идеально, а всё остальное на том же экране ползёт** —
значит потеря в том, что делается на процессоре каждый кадр. Второй:
холсты делятся не порядком рисования, а **частотой изменений** — живое
рядом со статикой заставляет перерисовывать и статику. Третий: у
SVG-персонажа платят не за количество записей, а за ФАКТ правки геометрии —
одна запись `d` стоит столько же, сколько сорок, поэтому экономить надо
кадры, а не записи (`docs/traps.md`, п. 45). Четвёртый: **на слое, который
меняется каждый кадр, не должно быть ни фильтра, ни маски, ни прозрачности
группы** — цена там не за то, что делает эффект, а за сам факт отдельного
буфера, и то же самое верно для всего живого ВНУТРИ такой группы
(`docs/traps.md`, п. 73; из-за этого обесцвечивание червя запекается в цвета,
а еда в кишке вынесена из-под маски тракта). Две главные потери
в игре нашлись трассировкой под замедлением и обе оказались не там, где их
искали бы чтением кода: бесконечная css-анимация внутри общего svg красила
всю сцену, а закрытые мини-игры продолжали крутить свои украшения
(`docs/traps.md`, пп. 36–38). Профиль снимается за минуту: playwright +
`Emulation.setCPUThrottlingRate` через CDP плюс `Tracing` — примеры в
истории правки 108.

**Правило направления: сторону проверяют, а не выводят в уме.** У svg ось Y
идёт ВНИЗ, и положительный `rotate()` уводит висящий конец ВЛЕВО — «плюс»
поворота противоположен привычному «вправо». Знак считается в понятиях игрока
(«куда уехал свободный конец»), переводится в поворот ОДИН раз на выходе, а
прогон проверяет СТОРОНУ, а не размах: проверка «лишь бы менялось» зелена при
любом знаке (`docs/traps.md`, п. 116).

**Правило замера: ждать устоявшееся, мерить и действовать по одному моменту.**
Плавающий провал, меняющий место от прогона к прогону, почти никогда не про
игру. Три причины, и все три — про то, КАК меряют: порог стоит ровно на
измеренном значении (запас в процент — это монетка, и признак того, что
меряют не ту величину); сетка задаёт вопрос НА ШВЕ (идти надо по серединам
клеток, а не по углам); замер сделан до того, как состояние нарисовалось
(персонаж считается на своей частоте, «доехало» становится правдой на кадр
раньше, чем узлы встают на места). Отличается от настоящего провала за
минуту: прогнать на базовом коммите и замерить САМУ ВЕЛИЧИНУ несколько раз —
стабильное число при красном результате означает порог, скачущее — замер на
ходу. Обездвиживать предмет измерения обязательно:
`setOptions({ wander: false, idleWave: false, blink: false })`
(`docs/traps.md`, п. 137).

**Правило чисел: сначала посчитать симулятором, потом добавлять контент.**
Три бесполезные или непроходимые механики были пойманы калькулятором за
минуту и ни одна — чтением кода (`docs/traps.md`, п. 11).

**Кадры на телефоне мерятся кнопкой «Слои».** Профайлера там нет, а замер на
компьютере отвечает на другой вопрос: движок другой, и расклад по стоимости у
него свой (в ванной размытие стоило семи кадров из восьми, а дождь — трёх
десятых; на айфоне вышло иначе). Кнопка гасит слои по одному — дождь,
персонаж, след, сцена, — рядом живой счётчик кадров: снял число, нажал ещё
раз. Правила гашения лежат рядом со сценой (пример — `lust.css`, блок
«гашение слоёв для замера»), классы вешаются на `<html>`.

Шкалы падают за 8–48 часов, вживую их не увидеть: кнопка 🐞 включает
debug-режим с отмоткой времени («−1 ч», «−8 ч», «Полные», «Сброс»).
Отматываются не системные часы, а метки в состоянии — проверяется ровно тот
механизм, что работает в игре. Свои панели есть у кухни
(`gluttony/kitchen-debug.js` — кладовая и перескоки по этапам), у сада
(`sloth/garden-debug.js` — отмотка грядок, семена, какашки) и у тщеславия
(`pride/pride-debug.js` — ступени трёх линий, гардероб, суточный счётчик и
числа следующего выхода). Общее у всех трёх одно: их предмет меряется
часами или месяцами и вживую не проверяется вовсе.

---

## 4. Структура

```
index.html          точка входа, подключение всех модулей
main.js             сборка HUD, маршрутизация по экранам (состояния НЕ хранит)
style.css           общие стили, safe-area
boot.js             версия сборки, service worker (не игровая логика)
sw.js               кеш сборки: отдаёт мгновенно, свежесть — через смену sw.js

src/config/         ВСЕ числа баланса и данные:
  economy.js          скорости шкал, награды, цены, валюты
  wrath-gear.js       слоты и предметы снаряжения гнева
  pride-wardrobe.js   слоты и предметы гардероба тщеславия
  kitchen.js          ингредиенты, жидкости, качество блюда
  garden.js           виды растений, ступени инструментов, времена этапов

src/core/           ядро, общее для всех экранов:
  boot-screen.js      когда снимать пелену (сама пелена — инлайном в <head>)
  events.js           шина событий (мини-игра → ядро)
  time.js             серверное время и ленивый расчёт шкал
  state.js            состояние игрока, форма повторяет будущую схему БД
  backend.js          переходник к «правде»: форма будущего API. ЕДИНСТВЕННОЕ
                      место, где что-то начисляется
  worm-*.js           модель персонажа, палитра, отметины, наряд

  ---- СТОПКА РЕНДЕРЕРА ----
  Был один файл на 7430 строк, и половину его занимала одна функция. Искать
  в нём существующее стоило дороже, чем написать рядом новое, — отсюда почти
  все «одно описано дважды» из docs/traps.md. Порядок ниже = порядок
  подключения:
  worm-basis.js       имена ступеней палитры, высоты черт лица, ЧИСЛА позы:
                      шаг цепочки, камера, доворот хвоста, разброс прогулки
  worm-room.js        комната и пол; перспектива одна на игру — roomScaleAt
  worm-paint.js       setAttr (не пишет то, что уже стоит), смешивание цвета,
                      детерминированный шум, ориентация тела
  worm-anatomy.js     слои под кожей (docs/worm-anatomy-layers.md)
  worm-head.js        череп, уши, глаза, пасть, поворот головы. Всё, что
                      зависит от ракурса, собирается ОДНОЙ функцией на вещь
                      (applyBrowTransform, applyHeadEarDepth, applyBrowFit) и
                      зовётся И на сборке, И при живом повороте: голова
                      пересобирается уже повёрнутой, и живой путь тогда не
                      срабатывает ни разу
  worm-marks-art.js   картинка отметины; ГДЕ она лежит — worm-marks.js
  worm-slime.js       след слизи на полу. Отдельная механика: знает только,
                      где червь и идёт ли он
  worm-hull.js        единый силуэт, кольца, кишечный тракт — всё, что
                      принадлежит телу целиком, а не отдельному звену
  worm-build.js       сборка червя в один узел, без покадровой жизни
  worm-renderer.js    монтаж на экран, ходьба, камера, tick, handle
  worm-silhouette.js  контур частей тела и разметка лица. ОДНО описание: по
                      нему рендерер строит путь, а размещение отметин
                      спрашивает «точка внутри?». Пока описаний было два
                      (кривые в рендерере, вписанный круг в размещении),
                      шрамы висели в воздухе рядом с головой
  worm-garment.js     кроильный станок: как ткань садится на часть тела.
                      Вещь объявляет ОХВАТ, а рисуется в координатах
                      выкройки, где ±1 поперёк — край части. Пока станка не
                      было, каждая из восьми вещей изобретала свои края и
                      промахивалась: фрак читался галстуком-бабочкой
  worm-cosmetics.js   как нарисован надетый наряд — ТОЛЬКО картинка, крой в
                      worm-garment.js. В ядре, а не в тщеславии: купленное
                      носится во ВСЕХ грехах
  loot-art.js         сундук с добычей. Держится на трёх вещах: горизонтальный
                      шов, ремни ПОПЕРЁК шва и НИЗКАЯ крышка — с высокой
                      получается шляпа
  token-art.js        жетон: круглая эмблема из трёх третей по три дольки.
                      Рисуется ВСЕГДА целиком, недостающее — пунктиром
                      (docs/token-art.md). Общий на четыре греха с жетонами
  worm-condition.js   довольность, истощение и смерть — формулой от меток
  rooms.js            локации данными: новая комната = запись в таблице
  minigame-window.js  общее окно: рамка, шапка, крестик, вопрос при выходе
  sins-menu.js        колесо грехов: удержание на червя, лучи, ворота награды
  liquid-stream.js    струя жидкости: один движок на все наливы в игре
  haptics.js          отдача в палец: Telegram, vibrate или ничего — решает
                      она, мини-игра только говорит, что произошло
  wallet-fx.js        прилёт и улёт в кошельке: цифра вылетает из счётчика
                      валюты при начислении и трате. Подписан на
                      GameState.addCurrency и находит счётчик по data-cur,
                      поэтому работает везде сам. Строк «−1 жетон» на
                      экранах заводить не надо
  worm-parts.js       таблица визуальных сущностей: имена для человека и
                      агента. Слой ПОВЕРХ рендерера — он о ней не знает,
                      убери файл, и ничего не изменится
  worm-inspect.js     инспектор: накладка поверх персонажа, ТРИ режима
                      (элемент / проблема / правка), шесть накладок, сбор
                      контекста для агента. Живёт внутри debug-режима
  worm-studio.js      студия: отдельный экран редактора. Лежит ВНУТРИ
                      #game-container — то есть в единицах холста и под его
                      масштабом; в <body> он верстался бы в пикселях экрана,
                      а их в Telegram всего 238. Свой неподвижный персонаж,
                      наезд камеры (viewBox корневого svg, НЕ css-масштаб),
                      выбор плитками по 44 единицы и ручки контура. Панель
                      показывает только УМЕСТНОЕ: ракурс лишь на голове,
                      откат лишь при наличии правок. Ничего не считает
                      сама — только инспектор, пульт и рендерер вместе
  worm-look.js        пульт: переводит человеческое намерение в числа
                      модели. Ничего не рисует; рендерер о нём не знает.
                      ЕДИНСТВЕННОЕ место, где внешность правится не кодом
  svg-space.js        перевод экран ↔ svg. БЕЗ getScreenCTM: учитывает ли он
                      трансформацию предка — вопрос браузера
  telegram.js         мост в Telegram: разворот окна, запрет свайп-закрытия.
                      Библиотека грузится только внутри Telegram
  stage.js            холст постоянного размера и «поверни телефон»
  debug.js            debug-режим и машина времени

src/minigames/<грех>/   каждая мини-игра в своей папке: js + css
  greed/                автомат: экран НИЧЕГО не решает — просит крутку у
                        Backend.greedSpin() и показывает три символа с
                        выплатой; веса и таблица выплат в конфиге
  wrath/                гнев: несколько экранов в одном окне. Фоны —
                        wrath-backdrop.js: у каждого режима своё МЕСТО, и оно
                        статическое. За списком живёт не обстановка, а
                        атмосфера (docs/traps.md, п. 86)
  gluttony/             кухня: магазин — kitchen-shop.js (телефон в углу
                        общего вида, приложение доставки: продукты за золото,
                        утварь за жетон); предметы по одному в kitchen-objects.js,
                        kitchen-art.js — игровой слой поверх них (что в кадре,
                        что интерактивно, где гнёзда); интерфейс диегетический
                        — предметы, а не меню; своя debug-панель
  sloth/                сад: участок ЧЕТЫРЬМЯ слоями глубины в garden-art.js
                        (небо, дальние поля, ближний фон, сами грядки), камера
                        ездит только по грядкам — стрелками, без панорамы;
                        растение выводится из вида и сида (plant-model.js);
                        магазин — sloth-shop.js, отдельный экран за корзиной
  lust/                 ванная: комната ЗАПЕЧЕНА из 3д (bath-baked.js —
                        файл сгенерированный, правятся модели в tools/),
                        bath-art.js — игровой слой поверх неё. Гнёзда — не
                        числа, а якоря запекания
docs/                   справочник по подсистемам (см. маршрутизатор)
docs/plan/              замысел каждого греха, экономика, метапрогрессия
tools/                  прогонные тесты, балансовые калькуляторы, сборочные
vendor/                 сторонние библиотеки, версия прибита в URL
```

Порядок подключения в `index.html` важен: конфиг → события → время →
состояние → переходник, и только потом `main.js` и мини-игры.

Новая мини-игра — отдельная папка, а не дописывание в существующие файлы.
Окно она не сочиняет: вешает `MinigameWindow.attach()` и кладёт внутрь только
своё содержимое. Одна игра из семи (зависть) ещё живёт на собственном
окне — это долг, а не образец для копирования.

Экран загрузки — исключение из правила «стили в css, логика в js»: его вид
написан инлайном в `<head>` index.html. Причина в комментарии там же: всё,
что подключается ссылкой, — это запрос в сеть ДО первого кадра, а пока он
идёт, экран белый.

---

## 5. Как правка попадает на телефон

Пуш в любую ветку → GitHub Actions собирает и выкладывает на Pages → ветка с
самым свежим коммитом лежит в корне сайта, её открывает иконка на домашнем
экране. Мержить в `main` ради просмотра не нужно. Разбор —
`docs/deploy-iphone.md`.

Отсюда требование к коду: **правка должна быть видна в браузере сразу.**
Никаких шагов сборки, транспиляции и npm-зависимостей в рантайме без крайней
необходимости.

Версии ассетов в `index.html` поднимать НЕЧЕМ: там у всех стоит
`?v=__BUILD__`, и метку подставляет деплой. Раньше это были числа,
которые правились руками, — работа впустую (деплой их всё равно
переписывает) и лишний повод забыть.

---

## 6. Стиль

* Комментарии по-русски, по делу: **почему так**, а не пересказ кода.
  Особенно там, где решение неочевидно или было выбрано после провала
  другого варианта. Комментарий «что сломалось и почему теперь иначе» стоит
  дороже десяти строк описания того, что и так видно.
* Цвет берётся из `src/core/palette.js`, толщины линий — из глобального
  `STROKE`. Хардкод hex в рендерере запрещён (`docs/art-direction.md`).
* Интерфейс без лишних кнопок: клики по самой сцене, графические крестики,
  понятные жесты.
* Правки вносятся точечно. В `README.txt` есть блок «Инструкция для ИИ» с
  требованием выдавать файлы целиком — он написан для чат-ассистентов без
  доступа к репозиторию и здесь не применяется.
* **В `docs/progress-log.md` пишется не всякая правка, а та, что меняет
  ПРАВИЛО или объясняет неочевидный выбор.** Что именно поменялось в коде —
  и так в git, и пересказывать его journal'ом незачем: файл вырос до девяти
  тысяч строк, и это налог на каждую правку, а не польза. Признак записи:
  через месяц кто-то спросит «почему это устроено так» — значит пиши.
  Признак не-записи: «переименовал, подвинул, поправил число» — не пиши.
* Ошибка, которая повторилась ВТОРОЙ раз или стоила переписывания, идёт в
  `docs/traps.md` — и обязательно в его оглавление, иначе её не найдут.
